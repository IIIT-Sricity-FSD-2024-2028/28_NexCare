node_modules/
back-end/node_modules/
front-end/node_modules/
.env

# Log files (runtime logs are written to back-end/logs — the folder is kept,
# the .log files themselves are not committed)
*.log
back-end/logs/*.log.*

# Uploaded documents — user content, not source
back-end/uploads/*
!back-end/uploads/.gitkeep

# Compiled backend output
back-end/dist/

# Second clone of this repo that used to live here — never commit it
28_NexCare/

# Lab 2 React deliverable — removed from the repo, never re-add it
Lab2-React/
lab2-react/
Lab02-React/

# Scratch test files
scratch/

# E2E data snapshot (created/removed by test/global-setup.js)
back-end/.e2e-data-backup/
