# Middleware Swagger Tests

This document outlines the procedures to test the NexCare middlewares and their respective access control, data constraints, and business logic using the interactive Swagger documentation.

## Test Cases for Middleware

### 1. Hospital Query & Access (`HospitalsController`)
**Target Endpoint**: `GET /api/hospitals/nearby`
- **Objective**: Verify that `x-query-timestamp` is correctly exposed.
- **Pre-requisite**: No authentication required.
- **Steps**:
  1. Open the `/api/hospitals/nearby` endpoint in Swagger.
  2. Input test data: `city = Tirupati`, `state = Andhra Pradesh`, `pincode = 517501`.
  3. Execute the request.
- **Expected Result**: 
  - Status Code: 200 OK
  - Headers should include `x-query-timestamp`.

**Target Endpoint**: `PUT /api/hospitals/{id}`
- **Objective**: Verify cross-hospital access denial (`HospitalAccessMiddleware`).
- **Pre-requisite**: Authenticate as a Hospital Manager for `H001` (e.g. `admin@nexcare.com` if assigned to H001).
- **Steps**:
  1. Login with a Hospital Manager account assigned to `H001`.
  2. Click Authorize and input the JWT token.
  3. Execute `PUT /api/hospitals/{id}` with `id = H002`.
- **Expected Result**: 
  - Status Code: 403 Forbidden
  - Message: "Cross-hospital access denied. You can only update your assigned hospital."

### 2. Bed Status Machine (`BedsController`)
**Target Endpoint**: `PATCH /api/beds/{id}/allocate`
- **Objective**: Verify illegal status transitions (`BedStatusChangeMiddleware`).
- **Steps**:
  1. Locate a bed with `status: MAINTENANCE`.
  2. Execute `PATCH /api/beds/{id}/allocate` on that bed.
- **Expected Result**:
  - Status Code: 400 Bad Request
  - Message indicating an invalid transition (since only AVAILABLE or CRITICAL beds can be allocated).

### 3. Ambulance Access (`AmbulanceController`)
**Target Endpoint**: `GET /api/ambulance/{id}`
- **Objective**: Verify patient-scoped access (`AmbulanceAccessMiddleware`).
- **Pre-requisite**: Authenticate as a Patient (e.g. `P001` or another patient ID).
- **Steps**:
  1. Login with a Patient account and set Bearer token in Swagger.
  2. Try to access an ambulance request belonging to *another* patient.
- **Expected Result**:
  - Status Code: 403 Forbidden
  - Message: "You can only access your own ambulance requests."

### 4. File Upload Limits (`UploadsController`)
**Target Endpoint**: `POST /api/uploads`
- **Objective**: Verify the 5MB file upload limit (`FileUploadMiddleware`).
- **Steps**:
  1. Login as Superuser (`superuser@nexcare.com`).
  2. Authorize in Swagger.
  3. Upload a file larger than 5MB on `POST /api/uploads`.
- **Expected Result**:
  - Status Code: 413 Payload Too Large
  - Message: "File exceeds the 5MB size limit."

## Notes
The Swagger documentation has been updated to reflect accurate return codes (400, 401, 403, 404, 413, etc.), include appropriate Request schemas, and provide real JSON mock IDs to allow true verification without altering application behavior.
\n## Global Middleware Test Cases (All Tags)\n\n### 5. Appointments Access & Limits\n**Target Endpoint**: `POST /api/appointments`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Appointments module.\n- **Pre-requisite**: Authenticate as `PATIENT / DOCTOR` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/appointments` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 6. Auth Access & Limits\n**Target Endpoint**: `POST /api/auth/register`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Auth module.\n- **Pre-requisite**: Authenticate as `PUBLIC` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/auth/register` endpoint in Swagger.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 7. Billing Access & Limits\n**Target Endpoint**: `POST /api/billing`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Billing module.\n- **Pre-requisite**: Authenticate as `ADMINISTRATIVE_STAFF` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/billing` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 8. Departments Access & Limits\n**Target Endpoint**: `POST /api/departments`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Departments module.\n- **Pre-requisite**: Authenticate as `SUPERUSER` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/departments` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 9. Equipment Access & Limits\n**Target Endpoint**: `POST /api/equipment`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Equipment module.\n- **Pre-requisite**: Authenticate as `HOSPITAL_MANAGER` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/equipment` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 10. Feedback Access & Limits\n**Target Endpoint**: `POST /api/feedback`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Feedback module.\n- **Pre-requisite**: Authenticate as `PATIENT` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/feedback` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 11. Hierarchy Access & Limits\n**Target Endpoint**: `PUT /api/hierarchy/{id}`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Hierarchy module.\n- **Pre-requisite**: Authenticate as `SUPERUSER` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `PUT /api/hierarchy/{id}` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 12. Hospital Details Access & Limits\n**Target Endpoint**: `PUT /api/hospital-details/{id}`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Hospital Details module.\n- **Pre-requisite**: Authenticate as `HOSPITAL_MANAGER` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `PUT /api/hospital-details/{id}` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 13. Inventory Access & Limits\n**Target Endpoint**: `POST /api/inventory`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Inventory module.\n- **Pre-requisite**: Authenticate as `HOSPITAL_MANAGER` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/inventory` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 14. Leaves Access & Limits\n**Target Endpoint**: `POST /api/leaves`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Leaves module.\n- **Pre-requisite**: Authenticate as `DOCTOR / STAFF` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/leaves` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 15. Logs Access & Limits\n**Target Endpoint**: `GET /api/logs`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Logs module.\n- **Pre-requisite**: Authenticate as `SUPERUSER` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `GET /api/logs` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n\n### 16. Notifications Access & Limits\n**Target Endpoint**: `PATCH /api/notifications/read-all`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Notifications module.\n- **Pre-requisite**: Authenticate as `ANY AUTHENTICATED` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `PATCH /api/notifications/read-all` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 17. Patients Access & Limits\n**Target Endpoint**: `PUT /api/patients/{id}`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Patients module.\n- **Pre-requisite**: Authenticate as `PATIENT` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `PUT /api/patients/{id}` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 18. Payments Access & Limits\n**Target Endpoint**: `POST /api/payments`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Payments module.\n- **Pre-requisite**: Authenticate as `PATIENT` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/payments` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 19. Revenue Access & Limits\n**Target Endpoint**: `GET /api/revenue`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Revenue module.\n- **Pre-requisite**: Authenticate as `SUPERUSER` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `GET /api/revenue` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n\n### 20. Schedules Access & Limits\n**Target Endpoint**: `POST /api/schedules`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Schedules module.\n- **Pre-requisite**: Authenticate as `DOCTOR` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/schedules` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 21. Support Requests Access & Limits\n**Target Endpoint**: `POST /api/support-requests`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Support Requests module.\n- **Pre-requisite**: Authenticate as `ANY AUTHENTICATED` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/support-requests` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 22. System Access & Limits\n**Target Endpoint**: `PUT /api/system/settings`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the System module.\n- **Pre-requisite**: Authenticate as `SUPERUSER` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `PUT /api/system/settings` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 23. Users Access & Limits\n**Target Endpoint**: `POST /api/users`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Users module.\n- **Pre-requisite**: Authenticate as `SUPERUSER` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/users` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n### 24. Wards Access & Limits\n**Target Endpoint**: `POST /api/wards`\n- **Objective**: Verify RBAC (401/403), CSRF Protection (403), and Rate Limiting (429) for the Wards module.\n- **Pre-requisite**: Authenticate as `HOSPITAL_MANAGER` (unless testing 401 Unauthorized).\n- **Steps**:\n  1. Open the `POST /api/wards` endpoint in Swagger.\n  2. **Test 401 Unauthorized**: Execute without setting the Bearer token.\n  3. **Test 403 Forbidden (RBAC)**: Login with an unauthorized role (e.g. if required is SUPERUSER, login as PATIENT), authorize, and execute.\n  4. **Test 403 Forbidden (CSRF)**: For this state-changing endpoint, Swagger handles the token, but you can simulate failure by stripping the `x-csrf-token` header.\n  5. **Test 429 Too Many Requests**: Execute this endpoint repeatedly in quick succession (typically > 300 hits/min, or > 20 hits/min for auth).\n- **Expected Results**:\n  - Status Code: 401 (Missing Token) / 403 (Invalid Role)\n  - Status Code: 403 (CSRF Token Missing/Invalid)\n  - Status Code: 429 (Rate Limit Exceeded)\n\n