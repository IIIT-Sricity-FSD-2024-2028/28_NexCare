# NexCare — Project Context

> Working context for anyone (human or AI) picking this repo up cold, mid-task,
> or after a break. **Read this before changing roles, actors, or naming.**
>
> Last full file-by-file sweep: **2026-08-28** (branch `vivian`, from commit `408624c`).
> Lab 2 React conversion + the cleanup listed in §14 landed the same day.

---

## 0. How to use this file

- §1–§5 are **rules and settled decisions**. Do not contradict them without the
  team agreeing first.
- §6–§13 are a **file-by-file inventory** of what exists and what it does. This is
  the "where was I" map — it is regenerated by sweeping the repo, so if you add or
  remove files, update the matching section.
- §14 is the **cleanup log** — what was broken, what got fixed, and what is still
  deliberately odd. Check it before you report a "bug" or "fix" a misspelt filename.
- §15 has the **commands and gotchas** you need to run anything.
- §16 covers **`Lab2-React/`**, the React conversion of the booking feature.

---

## 1. The one rule that governs everything

**NexCare is a strictly NON-CLINICAL platform.**

It digitises hospital *administrative* operations only: appointments and queues, bed
and ward allocation, ambulance coordination, inventory, billing, staff rosters,
feedback. It never touches diagnosis, treatment, prescriptions, or medical records.

Every naming and modelling decision follows from this. When in doubt, ask:
*"is this an administrative action or a clinical one?"* If clinical, it does not
belong in NexCare.

Source of truth for scope: `README.md`, `DomainExpertInteraction.md`, `definitions.yml`.

---

## 2. Where things stand right now

**Branch:** `vivian` — in sync with `origin/vivian` **and** `origin/main`
(all three at `408624c` as of 2026-08-28). Nothing to push.

**Backend test suite:** 5 suites, 19 tests, **all passing** (`npx jest` in `back-end/`,
~4s). Suites: `array.util`, `id-generator.util`, `hospitals.service`,
`hospital-query.interceptor`, `leave-request.guard`.

**Backend compiles clean.** It did not before 2026-08-28 — `npm run build` emitted
3 TypeScript errors (two wrong middleware import paths, one missing enum member).
Fixed; see §14.

**Lab 2 (React) is done.** `Lab2-React/` holds a React 19 + Vite 8 conversion of the
patient appointment feature — 15 components, an API layer, and a smoke test
(`npm test`). See §16.

**Recent work (last 5 commits, newest first):**

| Commit | What landed |
|---|---|
| `408624c` | Business-logic validations across multiple services |
| `a2726af` | Appointment validations + revenue calculation fix |
| `4410ed1` | Hospital registration + area-based approvals |
| `10b4cce` | Dynamic doctor availability + slot-conflict prevention in booking |
| `fc08dd0` | Superadmin reports + hospital registrations |

**Team branches:** `main`, `vivian`, `nikitha`, `poorna`, `deekshitha`, `Poorvishree`.
Top contributors by commit count: Vivian545 (74), Nikitha101021 (35), poorvishree (35),
Vivianmatheww (19), Deekshitha-1310 (13).

**Open work items:** none tracked. Next lab TBD.

---

## 3. Canonical actors

These four actors come from the domain expert interaction (31-01-2026) and are the
only ones the SRS recognises:

| Actor | Role value in code | Portal |
|---|---|---|
| Patient | `patient` | `front-end/patient/` |
| Administrative Staff | `administrative_staff` | `front-end/administrative_staff/` |
| Ambulance Staff | `ambulance` | `front-end/ambulance/` |
| Admin | `superuser` | `front-end/superuser/` |

The multi-hospital SaaS upgrade (commit `e6f3fc4`) added two oversight actors on top:

| Actor | Role value in code | Portal |
|---|---|---|
| Regional Officer | `regional_manager` | `front-end/regional-officer/` |
| Hospital Manager | `hospital_manager` | `front-end/hospital_manager/` |

### Naming decisions (settled 2026-08-26)

- **"Front Desk" is NOT a role.** It was only ever a display label for
  `administrative_staff`, and it was wrong. The role is displayed as
  **"Administrative Staff"** everywhere. "Front Desk" survives *only* as a
  department value alongside Management and Billing (`superuser/manage-users.js`).
- **The role stays `regional_manager` in code**, displayed as "Regional Officer".
  Do not rename the enum member; the display string is the thing users see.
- **Doctors and nurses are NOT actors.** See §4.

Role enum source of truth: `back-end/src/common/interfaces/api-response.interface.ts`.

---

## 4. Doctors: directory records, never actors

Doctors were wrongly built as a full login actor with their own portal. They are not
one — a doctor is a clinical role, and NexCare is non-clinical.

**The model now:**

- `UserRole.DOCTOR` and `UserRole.NURSE` still exist in the enum, but **only as
  directory records**. They are non-login. There is no doctor portal.
- They exist so an appointment can name *who the slot is with*, and so the
  regional/hospital views can count clinical headcount as an operational statistic.
- Directory records are created by the Admin via `superuser/manage-users.html`.
  They cannot self-register and cannot log in — both paths are blocked server-side.
  `AuthService.login` returns:
  `Access Denied: 'doctor' is a directory record, not a NexCare login account.`
- The `doctor` field on an appointment **stays named `doctor`**. It is booking
  metadata (which consultant the slot belongs to), not an actor reference.
  Deliberate decision — do not "clean this up" later.

**Do not** re-add: a doctor login option, a doctor portal, a doctor sidebar branch,
or doctor self-registration. (Two leftovers still exist — see §14.)

### Where staff leave lives now

The doctor portal owned leave requests. With no doctor portal, leave is administered
**by Administrative Staff on behalf of staff**, at
`front-end/administrative_staff/leave-requests.html`.

Authority is split, and this is deliberate:

- **Administrative Staff** record a leave request against a staff member and track it.
  They **cannot** approve or reject — `LeaveRequestGuard.validateLeaveApproval`
  rejects them with 403.
- **Hospital Manager / Superuser** approve or reject (`PATCH /api/leaves/:id`).

The backend `leaves` module is unchanged and still keys on `doctorId` / `doctorName`
(that is the directory record whose leave is being recorded). Its guard
(`leave-request.guard.ts`) blocks overlapping approved leaves and has passing specs —
**do not rekey those fields casually**, you will break `leave-request.guard.spec.ts`.

---

## 5. What the Regional Officer actually does

Role `regional_manager`. Oversight of a *set* of hospitals — one level below the
Admin/superuser, one level above a single hospital's staff.

**Backend powers (enforced by `@Roles`):**

| Endpoint | File |
|---|---|
| `PUT /hospitals/:id` — edit hospital details | `hospitals.controller.ts:65` |
| `GET /hospitals/review-queue` — registrations assigned to them | `hospitals.controller.ts:43` |
| `PATCH /hospitals/:id/regional-review` — clear or reject due diligence | `hospitals.controller.ts:91` |
| `PATCH /hospitals/:id/reject` — routed to `recordRegionalReview(…, 'rejected')` | `hospitals.controller.ts:82` |
| `GET /support-requests` — across their hospitals, filterable to one | `support-requests.controller.ts:11` |
| `PUT /support-requests/:id` — action/resolve a support request | `support-requests.controller.ts:37` |
| file uploads | `uploads.controller.ts:35` |

Explicitly **not** allowed: `PATCH /hospitals/:id/assign-manager` (superuser-only)
and `PATCH /hospitals/:id/verify` (superuser-only).

### The hospital approval chain (two-stage, deliberate)

```
POST /hospitals/register            (public)
   → verificationStatus = pending_verification
     regionalReviewStatus = pending

PATCH /hospitals/:id/assign-manager (superuser)
   → sets assignedManagerId

PATCH /hospitals/:id/regional-review (regional_manager, must be the assigned one)
   → regionalReviewStatus = 'cleared' | 'rejected'
   → NEVER activates the hospital

PATCH /hospitals/:id/verify         (superuser)
   → refuses unless assignedManagerId is set AND regionalReviewStatus === 'cleared'
   → verificationStatus = verified
```

A regional officer clearing a registration is due diligence, not activation.

**Frontend (`front-end/regional-officer/`):**

- `dashboard.html` — four KPI tiles (Assigned Hospitals, Total Doctors, Available
  Beds, Low Stock Items) plus a "Hospitals Under Me" table with occupancy.
- `hospital-details.html` — read-only drill-down, five tabs: Overview, Staff &
  Doctors, Beds & Wards, Inventory, Ambulances.
- `hospital-approvals.html` — approve/reject pending hospital registrations.
- `support-requests.html` — triage requests and advance their status.
- Login at `auth/regional-officer-login.html`.

**Read access is scoped, writes are not granted.** `GET /users` filters server-side
to the hospitals assigned to the officer (`users.controller.ts`), so they never see
staff elsewhere. `beds`, `inventory` and `ambulance` grant read via method-level
`@Roles` and are scoped client-side — the same posture `administrative_staff` already
had. Method-level `@Roles` fully overrides the class-level decorator (the guard uses
`getAllAndOverride`), which is how read access was added without granting writes.

---

## 5A. The revenue model (added 2026-08-29)

NexCare has **two revenue streams and they must never be conflated.** Getting this
wrong is the easiest way to build the wrong thing here.

### 1. Platform revenue — NexCare's own money

Hospitals pay NexCare to use the platform. Three components per hospital per cycle:

```
platform revenue = plan base fee
                 + (contracted beds − plan's included beds) × per-extra-bed fee
                 + commission rate × what the hospital COLLECTED
```

Plans live in `data/subscription-plans.json`, hospital placement in
`data/hospital-subscriptions.json`.

| Plan | Base/month | Included beds | Extra bed | Commission |
|---|---|---|---|---|
| Starter | ₹15,000 | 50 | ₹120 | 1.0% |
| Professional | ₹40,000 | 150 | ₹100 | 1.5% |
| Enterprise | ₹75,000 | 300 | ₹80 | 2.0% |

**Visible to the Admin/superuser only.** No hospital sees what the platform earns,
or what other hospitals pay.

### 2. Operational revenue — a hospital's own money

What a hospital collects from patients through NexCare billing. Belongs to that
hospital and its oversight chain, never to the platform's P&L.

### Who sees what

| Role | Sees | Where |
|---|---|---|
| Admin (superuser) | Platform revenue: MRR, ARR, commission, plan mix, per-hospital contribution. Can reprice plans and move hospitals between them. | `superuser/revenue.html` |
| Regional Officer | Operational revenue compared across **their assigned hospitals only** | `regional-officer/revenue.html` |
| Hospital Manager | Their own hospital's collections + what it owes NexCare | Revenue tab in `hospital_manager/dashboard.html` |
| Administrative Staff | Their own hospital's revenue (they raise the bills) | `GET /revenue/hospital/:id` |
| Patient | Nothing. Patients see their own bills, never revenue. | — |

Enforced in `revenue.controller.ts`: the class-level `@Roles(SUPERUSER)` covers the
platform routes, and `assertMayReadHospital` checks the caller actually oversees the
hospital before returning its numbers. Verified matrix — platform routes are 403 for
every non-superuser; `/revenue/hospital/H001` is 200 for HM001 and H001 staff but 403
for regional officer M001, who manages H002/H003.

### Nothing is stored pre-aggregated

Every figure is derived at read time from `billing.json` + the subscription files, so
a total can never drift out of step with the bills the front desk actually raised.
**A bill counts as revenue only when `status === 'Paid'`** — pending bills are
outstanding receivables, not revenue.

### Attribution — the thing that had to be fixed first

Revenue is meaningless in a multi-hospital platform if nothing carries a
`hospitalId`. Before this work **bills, appointments and patients all lacked one.**
The booking wizard captured the hospital and then threw it away, because
`CreateAppointmentDto` declared neither `hospitalId` nor `hospitalName` and the
ValidationPipe stripped both. That also meant **every patient booking returned
400** — see §14. Both fields are now on the DTO, the interface, the service and
`shared/db.js`'s whitelist, and historical rows were backfilled.

---

## 6. Repository map

```
FFSD/
├── back-end/          NestJS 10 API, port 3001, global prefix /api
├── front-end/         Static HTML/CSS/JS, one folder per portal, no framework
├── Lab2-React/        Lab 2 — React 19 + Vite conversion of the booking feature
├── Database/          ER diagram + MySQL schema (design artefacts, NOT used at runtime)
├── Figma Design/      Figma links
├── videos/            Team video link
├── .claude/           Claude Code local permissions
└── *.md, *.yml, *.pdf Project documentation
```

**The runtime store is JSON files on disk** (`back-end/data/*.json`), not MySQL.
`Database/DBschema.sql` is a design deliverable from an earlier phase and does not
match the running model (it still has `Doctors`, `Treatments`, `Reminders`, `Alerts`
tables and a `MANAGEMENT` role). Do not treat it as the schema of record.

---

## 7. Root-level files

| File | What it is |
|---|---|
| `README.md` | Problem statement, the 4 canonical actors, planned features per actor. The public face of the project. |
| `PROJECT_CONTEXT.md` | This file. |
| `TEST_ACCOUNTS.md` | Seed login reference. **Partly stale** — see §14. |
| `DomainExpertInteraction.md` | 37-min expert interview (31-01-2026, 35 yrs hospital ops experience). Source of the non-clinical constraint, the actor list, and terms like Bed Blocking / Administrative Triage / SPOC. |
| `definitions.yml` | Domain glossary — 21 terms with definition, examples, aliases. Last updated 2026-02-03. |
| `SRS.pdf` | Software Requirements Specification (994 KB). |
| `docs-assembler-config.json` | Doc publishing config → `IIIT-Sricity-FSD-2024-2028/28_NexCare`, branch `master`, GitHub Pages URL. |
| `.gitignore` | Ignores `node_modules/`, `.env`, `*.log`, `back-end/uploads/*`, `back-end/dist/`, and `28_NexCare/` (a second clone that used to live here). |
| `package-lock.json` | 89 bytes, empty stub at root. Real lockfiles are per-package. |
| `Lab2-React/` | The Lab 2 deliverable. Self-contained npm package — see §16. |
| `Database/DBschema.sql` | 185-line MySQL DDL — design artefact, not the runtime model. |
| `Database/ER_diagram.png`, `Database/ERDiagram`, `Database/DB Schema.pdf` | ER design artefacts. |
| `Figma Design/FigmaDashboard.md` | Figma design + prototype links. |
| `Figma Design/Figma Designs.pdf` | Exported designs. |
| `videos/video.md` | Google Drive link to the team video. |
| `.vscode/settings.json` | Live Server on port 5501 + docs-assembler syntax colours. |
| `.claude/settings.local.json` | Claude Code allowlist for this repo. |

---

## 8. Backend architecture

**Stack:** NestJS 10, Express, TypeScript 5, `class-validator`/`class-transformer`,
`@nestjs/swagger`, `helmet`. No database driver — persistence is JSON files.

**Boot:** `src/main.ts`
1. Loads `.env` by hand (no `dotenv` dependency).
2. Warns if `JWT_SECRET` is missing or left at the shipped default.
3. CORS — permissive by default (reflect any origin) so the WSL/multi-host dev setup
   works; set `CORS_ORIGIN` to a comma-separated allowlist to lock down.
4. Global prefix `api`.
5. Body limit `MAX_BODY_BYTES` (default 1 MB) on json + urlencoded.
6. Global `ValidationPipe` — `whitelist`, `forbidNonWhitelisted`, `transform`,
   implicit conversion, custom `exceptionFactory` producing `{field, messages[]}`.
7. Global `AllExceptionsFilter`.
8. Swagger built and **written to `docs/swagger.json` on every boot**, served at
   `/api/docs`.
9. `app.init()`, then Express-level `notFoundMiddleware` + `errorHandlerMiddleware`
   (these catch what Nest's filter cannot: body-parser failures, middleware throws,
   unmatched routes).
10. Listens on `0.0.0.0:${PORT||3001}` and prints every reachable LAN URL.
11. `uncaughtException` / `unhandledRejection` / SIGINT / SIGTERM all flush the file
    logger before exiting.

### Request pipeline (order matters)

```
SecurityMiddleware        headers, rate limiting, payload size     app-level, forRoutes('*')
RequestLoggerMiddleware   request id, access + error log lines     app-level, forRoutes('*')
  ↓
AuthGuard    (APP_GUARD)  @Public() bypasses; else verify Bearer JWT
                          → request.user = {id,email,role,name,patientId,hospitalId}
RolesGuard   (APP_GUARD)  no @Roles ⇒ any authenticated user;
                          else user.role must be in the list (getAllAndOverride,
                          so a method-level @Roles fully replaces the class-level one)
  ↓
Route-scoped middleware   BedStatusChangeMiddleware  (beds status/allocate/release/PUT/PATCH)
                          HospitalAccessMiddleware   (hospitals/:id family)
                          FileUploadMiddleware       (POST /uploads)
  ↓
Interceptors              HospitalQueryInterceptor   (GET /hospitals, /hospitals/nearby)
                          InventoryAuditInterceptor  (PATCH /inventory/:id/restock, /use)
                          FileInterceptor('file')    (POST /uploads)
  ↓
ValidationPipe → Controller → Service → FileStore(JSON)
```

### Auth

Hand-rolled JWT (HMAC via node `crypto`, no `jsonwebtoken` dependency).
Payload: `{sub, email, role, name, patientId, hospitalId, iat, exp}`.
Passwords: scrypt, stored as `scrypt$<saltHex>$<hashHex>`, compared with
`timingSafeEqual`. Legacy plaintext seed passwords are upgraded in place on first
login (see the gotcha in §15).

Login rejects, in order: unknown email → wrong password → role mismatch against the
role the form submitted → directory-only role (`doctor`/`nurse`) → inactive account.

---

## 9. Backend file inventory

### Feature modules — each is `X.module.ts` + `X.controller.ts` + `X.service.ts` (+ `dto/`, `interfaces/`)

| Module | Service LOC | Notes |
|---|---|---|
| `auth/` | 19 KB | JWT sign/verify, scrypt hashing, login, patient register, staff register, sessions, change-password. |
| `users/` | 10 KB | User CRUD; `GET /users` is hospital-scoped for `regional_manager`. `GET /users/doctors` feeds the booking wizard. |
| `patients/` | 13 KB | Patient records, search, blood-group and age-range queries. |
| `appointments/` | 20 KB | Booking with past-date, duplicate, slot-conflict and doctor-on-leave checks. Confirm/complete/cancel. Revenue = completed − cancelled. |
| `billing/` | 15 KB | Bills with CGST/SGST, payments, overdue, revenue. |
| `ambulance/` | 15 KB | Requests, dispatch, status ladder, per-patient and per-crew views. |
| `feedback/` | 12 KB | Ratings, categories, unresolved and high-priority views, status changes. |
| `beds/` | 12 KB | Bed CRUD, allocate/release/status, ward and occupancy stats. Has `middleware/bed-status-change.middleware.ts`. |
| `inventory/` | 19 KB | Stock, restock/use, low-stock and out-of-stock, per-item audit trail. Has `interceptors/inventory-audit.interceptor.ts`. |
| `hospitals/` | 10 KB | Registration, public search, nearby (proximity score pincode 3 > city 2 > state 1), two-stage approval. **Two controllers** — `hospitals.controller.ts` and `hospital-details.controller.ts` (the Regional Officer drill-down tabs, kept separate so other members' files stay untouched). Has `hospital-access.middleware.ts` + `hospital-query.interceptor.ts`. |
| `system/` | 12 KB | Activity/audit log + system settings. Every other service logs through it. |
| `leaves/` | 8 KB | Leave records keyed on `doctorId`/`doctorName`. `guards/leave-request.guard.ts` blocks overlapping approved leaves and restricts approval to hospital_manager/superuser. |
| `support-requests/` | 3 KB | Hospital→region escalation. Role-scoped read in the controller itself, not by `@Roles`. |
| `uploads/` | 5 KB | Multer file upload, download, delete. |
| `departments/`, `wards/`, `equipment/` | ~1 KB each | Read-only lookup lists, `hospitalId`-filterable. |

### Shared/common

| Path | Purpose |
|---|---|
| `common/interfaces/api-response.interface.ts` | `ApiResponse<T>` + **every status enum** and `UserRole`. The single source of truth for roles. |
| `common/guards/auth.guard.ts` | Bearer JWT → `request.user`. Honours `@Public()`. |
| `common/guards/roles.guard.ts` | `@Roles()` check via `getAllAndOverride`. |
| `common/decorators/public.decorator.ts` | `@Public()` — skips both guards. |
| `common/decorators/roles.decorator.ts` | `@Roles(...)`. |
| `common/utils/response.util.ts` | `ResponseUtil.success/created/updated/error/notFound/forbidden/serverError`. **Every service depends on this — do not change its shape.** |
| `common/utils/file-store.util.ts` | `FileStore<T>` — load/save `data/<file>.json`, seeds on first access. The whole persistence layer. |
| `common/utils/id-generator.util.ts` (+ spec) | ID and token generation. |
| `common/utils/array.util.ts` (+ spec) | Array helpers. |
| `common/utils/sanitizer.util.ts` | Input sanitising. |
| `common/filters/all-exceptions.filter.ts` | Global catch → standard envelope + error log. |
| `common/filters/http-exception.filter.ts` | Narrower HTTP filter. |
| `common/logging/file-logger.ts` | Buffered writer for `logs/access-*.log`, `app-*.log`, `error-*.log`. |
| `common/logging/logs.controller.ts` | `GET /logs`, `GET /logs/files` (superuser + administrative_staff). |
| `common/validation/*`, `common/services/validation.service.ts` | DTO validator helper, frontend validation service, worked examples. |
| `common/dto/` | `date-range.dto.ts`, `query-params.dto.ts`. |
| `lodger.middleware.ts` | **Misspelt on purpose — it is the real file.** Exports `RequestLoggerMiddleware` (aliased `LoggerMiddleware`, `LodgerMiddleware`), `SecurityMiddleware`, `FileUploadMiddleware`, `MAX_UPLOAD_BYTES` (5 MB), `errorHandlerMiddleware`, `notFoundMiddleware`. |
| `logger.middleware.ts` | One line: `export * from './lodger.middleware';` |
| `common/middleware/*.ts` | Three one-line re-export shims to the same file. |
| `src/dto/index.ts` + `src/dto/README.md` | Barrel export + DTO conventions doc. |

### Non-source backend files

| Path | Notes |
|---|---|
| `back-end/data/*.json` | **The database.** 18 files — see §11. |
| `back-end/docs/swagger.json` | 134 KB, regenerated on every server boot. Expect it dirty in `git status` after running the backend. |
| `back-end/logs/` | `README.md` is tracked; the `.log` files are gitignored. |
| `back-end/uploads/` | `.gitkeep` only; uploads gitignored. |
| `back-end/generate-swagger.ts` | Standalone swagger dump. **Prefer just booting the server** — this prints to stdout mixed with Nest logs and emits the same 106 paths in a different key order (huge meaningless diff). |
| `back-end/README.md` | Original module/endpoint doc from the scaffold phase. |
| `back-end/TEAM_HANDOFF.md` | Historical per-member work split (Deekshitha scaffold; Vivian RBAC+integration; Nikitha data+testing; Poornasri DTO+Swagger; Poorvishree ER+service logic). Mostly done — read as history, not as a todo list. |
| `back-end/dist/` | Gitignored and no longer tracked (4 stray files were untracked on 2026-08-28). Regenerate with `npm run build`. |

---

## 10. Complete route map

Every route is under the global prefix **`/api`**. "class" = the controller-level
`@Roles`; a method row that lists roles **replaces** the class list entirely.

### `auth` — no class-level roles
| Method | Path | Roles |
|---|---|---|
| POST | `/auth/login` | **Public** |
| POST | `/auth/register` | **Public** (patients only) |
| POST | `/auth/register-staff` | **Public** (administrative_staff + ambulance only) |
| POST | `/auth/logout/:userId` | any authenticated |
| GET | `/auth/current/:userId` | any authenticated |
| GET | `/auth/sessions` | superuser |
| PATCH | `/auth/change-password` | any authenticated |

### `users` — class: superuser, administrative_staff
| Method | Path | Roles |
|---|---|---|
| GET | `/users` | + regional_manager (server-side hospital scoping) |
| GET | `/users/doctors` | superuser, administrative_staff, **patient** |
| POST · PUT · PATCH · DELETE | `/users`, `/users/:id`, `/users/:id/status` | class |
| GET | `/users/stats/overview`, `/users/role/:role`, `/users/search/:query`, `/users/:id` | class |

### `patients` — class: superuser, administrative_staff
| Method | Path | Roles |
|---|---|---|
| GET | `/patients`, `/patients/stats/overview`, `/patients/search/:query`, `/patients/blood-group/:bloodGroup`, `/patients/age-range` | class |
| POST | `/patients` | class |
| GET · PUT · PATCH | `/patients/:id` | + **patient** |
| DELETE | `/patients/:id` · PATCH `/patients/:id/status` | class |

### `appointments` — class: superuser, administrative_staff, doctor
| Method | Path | Roles |
|---|---|---|
| GET | `/appointments` | + **patient** |
| POST | `/appointments` | superuser, administrative_staff, **patient** |
| GET | `/appointments/patient/:patientId`, `/appointments/:id` | + **patient** |
| GET | `/appointments/stats/overview`, `/department/:department`, `/today` | class |
| PUT · PATCH · DELETE | `/appointments/:id` | class |
| PATCH | `/appointments/:id/confirm`, `/complete` | class |
| PATCH | `/appointments/:id/cancel` | superuser, administrative_staff, **patient** |

### `billing` — class: superuser, administrative_staff
`GET /billing`, `GET /billing/patient/:patientId`, `GET /billing/:id`,
`PATCH /billing/:id/pay` also allow **patient**.
Others (`POST`, `PUT`, `PATCH`, `DELETE`, `/stats/overview`, `/overdue`, `/revenue`) are class-only.

### `ambulance` — class: superuser, administrative_staff, ambulance
| Method | Path | Roles |
|---|---|---|
| GET | `/ambulance` | + **patient**, **regional_manager** |
| POST · DELETE | `/ambulance`, `/ambulance/:id` | + **patient** |
| GET | `/ambulance/patient/:patientId`, `/ambulance/:id` | + **patient** |
| GET | `/ambulance/stats/overview`, `/active`, `/assigned/:assignedTo` | class |
| PUT · PATCH | `/ambulance/:id`, `/:id/dispatch`, `/:id/complete`, `/:id/status` | class |

### `feedback` — class: superuser, administrative_staff
`GET /feedback`, `POST /feedback`, `GET /feedback/patient/:patientId` also allow **patient**.
`/stats/overview`, `/category/:category`, `/rating/:rating`, `/unresolved`,
`/high-priority`, `/:id` (GET/PUT/PATCH/DELETE), `/:id/status` are class-only.

### `beds` — class: superuser, administrative_staff
`GET /beds` also allows **regional_manager**. Everything else class-only:
`POST`, `/stats/overview`, `/ward/:ward`, `/available`, `/patient/:patient`,
`/occupancy`, `/:id` (GET/PUT/PATCH/DELETE), `/:id/allocate`, `/:id/release`, `/:id/status`.

### `inventory` — class: superuser, administrative_staff
`GET /inventory` also allows **regional_manager**. Everything else class-only:
`POST`, `/stats/overview`, `/low-stock`, `/out-of-stock`, `/category/:category`,
`/location/:location`, `/search/:query`, `/audit/:itemId`, `/:id` (GET/PUT/PATCH/DELETE),
`/:id/restock`, `/:id/use` (last two audited).

### `hospitals` — `HospitalsController`, no class-level roles
| Method | Path | Roles |
|---|---|---|
| GET | `/hospitals` | **Public** (+ HospitalQueryInterceptor) |
| GET | `/hospitals/nearby` | **Public** (+ HospitalQueryInterceptor) |
| GET | `/hospitals/:id` | **Public** |
| POST | `/hospitals/register` | **Public** |
| GET | `/hospitals/review-queue` | regional_manager |
| PUT | `/hospitals/:id` | superuser, regional_manager, hospital_manager |
| PATCH | `/hospitals/:id/verify` | superuser (gated on regional clearance) |
| PATCH | `/hospitals/:id/reject` | superuser, regional_manager |
| PATCH | `/hospitals/:id/regional-review` | regional_manager |
| PATCH | `/hospitals/:id/assign-manager` | superuser |

### `hospitals` — `HospitalDetailsController` — class: superuser, regional_manager, hospital_manager, administrative_staff
`GET /hospitals/:id/doctors`, `/beds`, `/inventory`, `/ambulances`.

### `support-requests` — no class-level roles, scoped in code
| Method | Path | Behaviour |
|---|---|---|
| GET | `/support-requests` | superuser → any; regional_manager → their hospitals (filterable); hospital_manager → **locked to own hospital, client `hospitalId` ignored**; staff → own hospital |
| POST | `/support-requests` | any authenticated; `hospitalId` defaults to the caller's |
| PUT | `/support-requests/:id` | superuser, regional_manager, hospital_manager |

### `leaves` — class: superuser, hospital_manager, administrative_staff, doctor
`GET /leaves`, `/leaves/calendar`, `/leaves/:id`, `POST /leaves`,
`PATCH /leaves/:id`, `DELETE /leaves/:id`.
`LeaveRequestGuard` narrows POST (no overlapping approved leave) and PATCH
(approve/reject is hospital_manager + superuser only — administrative_staff gets 403).

### `system` — class: superuser, administrative_staff
`GET /system/activity`, `POST /system/activity`, `GET /system/settings`,
`/settings/key/:key`, `/stats`, `/activity/date-range`, `/activity/user/:userId`,
`/activity/recent`, `/settings/category/:category`, `/activity/search/:query`,
`/activity/:id`, `/settings/:id`, `PUT /system/settings`, `PUT /system/settings/:id`.

### `uploads` — class: superuser, administrative_staff, hospital_manager, regional_manager
`POST /uploads` (FileInterceptor, 5 MB cap), `GET /uploads`, `GET /uploads/:id`,
`GET /uploads/:id/download`, `DELETE /uploads/:id` (**superuser + administrative_staff only**).

### `logs` — class: superuser, administrative_staff
`GET /logs`, `GET /logs/files`.

### lookups — class: superuser, administrative_staff, regional_manager
`GET /departments`, `GET /wards`, `GET /equipment` — all accept `?hospitalId=`.

---

## 11. The data store (`back-end/data/`)

Every service persists through `FileStore<T>`, so **all 18 files survive a restart**.

| File | Rows | Key fields |
|---|---|---|
| `users.json` | 45 | `id, name, email, role, status, password` (+ `hospitalId, patientId, dept, city, state, pincode` where relevant) |
| `patients.json` | 15 | `id, fullName, phone, email, patientIdDisplay, memberSince, status, bloodGroup, age` |
| `appointments.json` | 13 | `id, patientId, patientName, department, doctor, dateLabel, timeLabel, token, fee, status, reason` |
| `hospitals.json` | 12 | 36 fields incl. `verificationStatus, regionalReviewStatus, assignedManagerId, specialities, totalBeds, availableBeds, lat/long` |
| `beds.json` | 75 | `id, ward, status, patient, hospitalId` |
| `billing.json` | 9 | `id, patientId, subtotal, cgstRate, sgstRate, total, items[], payments[]` |
| `inventory.json` | 36 | `id, name, category, quantity, minStock, unit, location, status, hospitalId` |
| `inventory-audit.json` | 7 | `id, itemId, action, quantityBefore, quantityAfter, userId, timestamp, notes` |
| `ambulance.json` | 5 | `id, patientId, pickupLocation, contact, status, assignedTo, hospitalId` |
| `feedback.json` | 5 | `id, patientId, sender, type, category, subject, summary, rating, status` |
| `leaves.json` | 4 | `id, doctorId, doctorName, hospitalId, startDate, endDate, reason, status, approvedBy` |
| `support-requests.json` | 5 | `id, hospitalId, createdBy, assignedManagerId, category, subject, priority, status` |
| `system-activity.json` | 53 | `id, timestamp, userId, action, details, module, severity` |
| `system-settings.json` | 5 | `id, key, value, description, category` |
| `departments.json` | 17 | `id, name, hospitalId` |
| `wards.json` | 19 | `id, name, hospitalId` |
| `equipment.json` | 20 | `id, name, type, status, hospitalId` |
| `uploads.json` | 0 | populated at runtime |

### Actual seed roster (verified 2026-08-28 — supersedes `TEST_ACCOUNTS.md`)

45 users: 1 superuser, 1 regional_manager, 1 hospital_manager, 5 administrative_staff,
3 ambulance, 15 patients, **19 doctors** (directory-only).

Login accounts worth knowing — password `Password123` for all:

| Role | Name | Email | Hospital |
|---|---|---|---|
| superuser | Rajesh Kumar | `superuser@nexcare.com` | — |
| regional_manager (`M001`) | Rajesh Sharma | `regional@nexcare.com` | — |
| hospital_manager (`HM001`) | Srinivas Rao | `hospitalmanager@nexcare.com` | H001 |
| administrative_staff | Priya Reddy | `admin@nexcare.com` | H001 |
| administrative_staff | Lakshmi Menon | `lakshmi@nexcare.com` | H002 |
| administrative_staff | Divya Krishnan / Karthik Raman | `divya@` / `karthik@nexcare.com` | H003 |
| administrative_staff | Anita Joshi | `anita@nexcare.com` | H001 |
| ambulance | Alex Martinez / Suresh Babu / Manoj Selvam | `ambulance@` / `suresh@` / `manoj@nexcare.com` | H001 / H002 / H003 |
| patient | John Anderson | `patient@gmail.com` | — |
| patient | +14 more (`patient2..8@gmail.com`, `ananya.sharma@`, `rahul.verma@`, …) | | |

**Hospitals:** H001 NexCare AIIMS Super Speciality (Tirupati, mgr HM001) ·
H002 Apollo Health City (Tirupati, mgr M001) · H003 Fortis Care (Chennai, mgr M001) ·
H004 Sri Venkateswara Care Center (Tirupati, no mgr) · HSP001–HSP008 (Tirupati,
Chittoor, Renigunta, Nellore — all verified, no manager).

**Login pages:** `auth/superuser-login.html` · `auth/regional-officer-login.html` ·
`auth/staff-login.html` (administrative_staff + ambulance) ·
`auth/hospital-manager-login.html` · `auth/patient-login.html` ·
`auth/login.html` (combined).

---

## 12. Frontend architecture

Plain HTML + CSS + vanilla JS, served statically (`npx serve` on 8080, or VS Code
Live Server on 5501). No build step, no bundler, no framework.

### The shared layer — `front-end/shared/`

| File | Size | Role |
|---|---|---|
| `api.js` | 21 KB | **The API client.** `NexCareAPI` class + 13 modules (`Auth`, `Users`, `Patients`, `Appointments`, `Billing`, `Ambulance`, `Feedback`, `Beds`, `Inventory`, `Hospitals`, `Leaves`, `SupportRequests`, `System`) exposed on `window.NexCareAPI`, plus legacy flat aliases. Resolves the backend URL as `http://<current hostname>:3001/api` so it works on localhost, LAN IP, or WSL IP. Normalises every response to `{success, data, message}`. Also exports `window.pageLink(page, params)` — builds in-app links that keep `.html` only when the current URL has it, because static hosts 301 `/page.html?x=1` → `/page` and **drop the query string**. |
| `session.js` | 12 KB | **Auth + routing guard.** Hides `<html>` before paint, decodes the JWT client-side (expiry only — signature is the backend's job), redirects unauthenticated users off protected pages, enforces `rolePathMap` so a role cannot sit in another portal, fades the page in, injects the floating back button. Exports `loginUser`, `redirectByRole`, `logoutUser`. |
| `db.js` | 35 KB | **API-first compatibility bridge.** `NexCareDB` / `NexCareStore` try the API and fall back to `localStorage` (`nexcare_db_v3`) with a small seed. This is why the portals still work with the backend down. |
| `nav.js` | 11 KB | Injects the sidebar and the role display name. **Only the superuser and regional-officer portals actually load it** — see §14. |
| `ui-components.js` | 11 KB | `NexCareUI` — success modal, toast, confirm. `api.js` calls `NexCareUI.showToast` on network errors. |
| `mock-hospitals.js` | 30 KB | `window.MOCK_HOSPITALS` — hospital → departments → doctors → `availableDays` → `slots`. **The booking wizard's step 0–2 data source.** |
| `mockdb.json` | 7 KB | Legacy seed JSON. |
| `global.css`, `nav.css` | 11 + 3 KB | Base styles and sidebar styles. |

`front-end/logo.js` (85 lines) defines the `<nex-care-logo>` custom element with
Shadow DOM so its styles never leak.

### Portals

| Portal | Pages | Sidebar | Notes |
|---|---|---|---|
| `patient/` | dashboard, appointments, billing, ambulance, feedback, profile, hospital-search | hardcoded in HTML | Biggest CSS (`styles.css` 58 KB). `dataStore.js` is a thin ES-module proxy to `window.NexCareStore`. The 3 legacy `book-appointment-step*.html` pages were removed on 2026-08-28. |
| `administrative_staff/` | dashboard, bed-allocation, inventory, manage_appointments, patient-directory, patient_checkin, staff_scheduling, generate-bill, feedback, leave-requests, system-logs | hardcoded in HTML | Each page has a matching `.js`; most define their own local `apiGet`/`apiRequest` rather than using `shared/api.js`. |
| `superuser/` | dashboard, hospital-registrations, patient-directory, manage-users, system-settings, feedback, reports | **`shared/nav.js`** | `reports.js` is the largest (594 lines): usage, performance, operational, security tabs with hand-drawn charts. |
| `regional-officer/` | dashboard, hospital-approvals, hospital-details, support-requests | **`shared/nav.js`** | `hospital-details.html` has the 5 read-only tabs. |
| `hospital_manager/` | dashboard | hardcoded | 483-line tabbed dashboard: overview, leaves (approve/reject), staff, support. |
| `ambulance/` | index.html | hardcoded | Single-page app: 6 `data-page` views (dashboard, ambulance-requests, assigned-dispatch, active-transport, completed-transports, profile). `app.js` is **3522 lines / 135 KB** — the largest file in the repo. |
| `auth/` | login, patient-login, patient-register, staff-login, staff-register, superuser-login, regional-officer-login, hospital-manager-login, signup | n/a | |
| `landing/` | landing.html (64 KB, sections: home, features, roles, how-it-works, reviews, contact), hospital-registration.html | n/a | `hospital-registration.html` is now the **only** hospital sign-up page; `auth/login.html` links here. |

### The patient booking wizard (the React-conversion candidate)

`patient/appointments/appointments.html` is one page with three top-level sections
toggled by `display`: `#appointmentLanding`, `#myAppointments`, `#bookingFlow`.
`appointments.js` (1069 lines) drives it with two module-level variables:

```js
let bookingData = { hospital, department, doctorId, doctor, date, time, patientInfo };
let currentStep = 0..4;
```

`renderBookingStep()` dispatches to `renderStep0` (hospital) → `renderStep1`
(department) → `renderStep2` (date & doctor) → `renderStep3` (details) →
`renderConfirmation`. `getBookedSlotsForDoctor()` unions booked slots from three
sources (API → `NexCareStore` → raw `localStorage`) to grey out taken times.

That shared `bookingData` is exactly the "lift state up to the common parent" the lab
asks for, and each step becomes a child component receiving props + an `onSelect`
callback.

---

## 13. Frontend file counts

29 files in `administrative_staff/` · 16 in `patient/` (+2 in `patient/appointments/`) ·
15 in `superuser/` · 11 in `auth/` · 9 in `shared/` · 8 in `regional-officer/` ·
4 in `landing/` · 4 in `ambulance/` · 3 in `hospital_manager/` · 2 in `assets/`.

**Every relative `href`/`src` across all 47 HTML pages resolves** (audited
2026-08-28; re-run the audit after adding pages).

Also: `front-end/debug.html` (5 KB dev scratch page), `front-end/jsconfig.json`
(`checkJs: false`), `front-end/package-lock.json` (88-byte stub),
`front-end/administrative_staff/generate_pages.js` (257 lines, page-generation helper
loaded by 0 pages — still dead, left in place deliberately).

---

## 14. Cleanup log and remaining traps

### Fixed on 2026-08-28

The 2026-08-28 sweep found these; they have all been dealt with. Kept here so
nobody "rediscovers" them or reverts a fix by accident.

**Backend would not compile** (`npm run build` emitted 3 TS errors while still
emitting output, so it looked like it worked):

1. `beds/beds.module.ts` imported `BedStatusChangeMiddleware` from
   `../lodger.middleware`, which does not export it → now imports from
   `./middleware/bed-status-change.middleware`.
2. `hospitals/hospitals.module.ts` had the same wrong import for
   `HospitalAccessMiddleware` → now `./middleware/hospital-access.middleware`.
3. `ambulance.service.ts:91` referenced `AmbulanceStatus.CANCELLED`, missing from
   the enum. **Added it** (every sibling status enum has one) and completed the two
   exhaustive `Record<AmbulanceStatus, …>` maps it cascaded into — `getStats()`'s
   counter and `isValidStatusTransition()`'s table. A trip may now be cancelled from
   Pending / Dispatched / En Route; once the patient is picked up it runs to
   completion. `Completed` and `Cancelled` are both terminal.
   Note: cancelling an ambulance request is still a **hard delete** in
   `ambulance.service.ts:161` (`splice`), so nothing writes `'Cancelled'` yet. The
   enum member exists for `PATCH /ambulance/:id/status` and for the duplicate check.

**Dead links** — `shared/nav.js` pointed at two files that never existed
(`administrative_staff/reports.html`, `patient/appointments.html`), and
`patient/appointments/appointments.html` linked its sidebar brand to `dashboard.html`
which resolved *inside* the appointments folder. All three fixed.

**Doctor login removed** — the `value="doctor"` radio in `auth/login.html` and
`auth/staff-login.html`, the `case "doctor"` redirect and `rolePathMap` entry in
`shared/session.js`, and the orphaned `front-end/doctor/` portal are gone. This was a
dead end: the backend always refused it. §4 is now true in the UI as well.
The "Doctor (directory record)" option in `superuser/manage-users.html` **stays** —
that creates a directory record, which is correct.

**Duplicates and orphans removed** — `front-end/manager/` (superseded by
`hospital_manager/`, nothing linked to it); `front-end/hospital-registration/`
(consolidated onto `landing/hospital-registration.html`, which is the version that
sends the `adminPhone` the backend requires — `auth/login.html` and the
`session.js` public-path list were repointed); the three orphaned
`book-appointment-step*.html` pages (nothing outside them linked in, superseded by
the single-page wizard, and they referenced an `app.js` that never existed);
`back-end/.next/` (4 Next.js bundles from an abandoned experiment);
4 stray tracked files under the gitignored `back-end/dist/`;
`patch_ambulance*.py` (operated on a clone path that no longer exists);
`front-end/frontend_log.txt`.

**`TEST_ACCOUNTS.md` rewritten** — it claimed 12 seed accounts, 5 login actors,
7 doctors and 2 hospitals. Reality: **45 users, 26 login accounts, 19 doctors,
12 hospitals**. It also claimed wards/departments/equipment do not persist; they do.

### Still true, still deliberate

**`lodger.middleware.ts` is misspelt on purpose** — it is the real file.
`logger.middleware.ts` and the three `common/middleware/*.ts` files are one-line
re-export shims to it. Do not "fix" the name without updating all four shims,
`app.module.ts`, and `main.ts`. (The two bad imports fixed above were *not* this
file's fault — those middlewares genuinely live in their own feature folders.)

**Only superuser and regional-officer load `shared/nav.js`.** Every other portal
hardcodes its sidebar in HTML. The `patient` and `administrative_staff` branches of
`nav.js` are therefore still dead code — but their links are now correct, so wiring
those portals up later is a one-line change rather than a debugging session.

**`Database/DBschema.sql` does not describe the running system.** It is a design
artefact with `Doctors`, `Treatments`, `Reminders`, `Alerts` tables and a
`MANAGEMENT` role that no longer exist in the model. The runtime store is
`back-end/data/*.json`.

**`administrative_staff/generate_pages.js`** (257 lines) is loaded by zero pages.
Left in place — it looks like a scaffolding helper someone may still want.

---

## 15. Conventions, commands, gotchas

### Conventions
- Commits: Conventional Commits (`feat:`, `fix:`, `build:`).
- Branch per member (`vivian`, `nikitha`, `poorna`, `deekshitha`, `Poorvishree`),
  merged to `main`.
- **Never add a Claude co-author trailer to commits.**
- `back-end/dist/` and `back-end/docs/swagger.json` are generated — rebuild rather
  than hand-edit.

### Running it

```bash
# Backend (port 3001)
cd back-end
npm install
npm run start:dev          # watch mode
npm run start:prod         # node dist/src/main
npx jest                   # 5 suites, 19 tests
npm run build              # regenerate dist/

# Frontend (any static server; the API client mirrors the hostname on :3001)
cd front-end
npx serve -l 8080
```

Swagger UI: `http://localhost:3001/api/docs`.
**Just start the server to regenerate the docs** — `main.ts` writes
`docs/swagger.json` during bootstrap, so it is always current after a run, and that
is the authoritative version. Prefer it over `generate-swagger.ts`.

### Env vars
`PORT` (3001) · `JWT_SECRET` (warns if unset or left at the shipped default) ·
`CORS_ORIGIN` (comma list; default reflects any origin) · `MAX_BODY_BYTES` (1 MB) ·
`MAX_UPLOAD_BYTES` (5 MB) · `API_URL`. Loaded by a hand-rolled parser in `main.ts`;
`.env` is gitignored.

### Gotcha: running the backend dirties the repo
`docs/swagger.json` is rewritten on every boot. Check the diff is real before
committing.

### Gotcha: login rewrites the password before authorising
`AuthService.login` upgrades a legacy plaintext password to a scrypt hash *before*
it checks the role. A login that is ultimately rejected still rewrites that user's
stored password in `data/users.json`. Harmless (the password was verified correct
first), but it means poking at auth dirties the seed data — check
`git diff back-end/data/` before committing.

### Gotcha: static hosts eat query strings
`npx serve` answers `/page.html?x=1` with a `301 → /page` and **drops the query
string**. Always build in-app links with `window.pageLink(page, params)` from
`shared/api.js` instead of hardcoding `.html`.

### Gotcha: method-level `@Roles` replaces, not extends
`RolesGuard` uses `reflector.getAllAndOverride`. A method-level `@Roles(...)` wipes
out the class-level list entirely — so a method list must repeat every role that
should still have access. This is how read access was granted to `regional_manager`
without granting writes.

---

## 16. `Lab2-React/` — the React conversion

Lab 2 deliverable, added 2026-08-28. A **React 19 + Vite 8** conversion of the
patient appointment feature. It is a self-contained npm package and does not
affect the vanilla portals in `front-end/` — nothing there imports from it.

**What was converted:** the 4-step booking wizard plus "My Appointments", i.e.
`front-end/patient/appointments/appointments.{html,js}` (1069 lines of template
strings driven by two module-level variables, `bookingData` and `currentStep`).

**Component tree** — 15 components, ~1,100 lines:

```
App.jsx                    owns ALL shared state, renders the shell
├── LoginPanel             patient sign-in (local form state)
├── StepIndicator          progress bar (props only, raises nothing)
├── BookingWizard          step router, owns no state
│   ├── HospitalStep   → HospitalCard        step 0
│   ├── DepartmentStep                       step 1
│   ├── DoctorDateStep → DoctorCard,         step 2
│   │                    SlotPicker
│   ├── DetailsStep                          step 3
│   └── Confirmation                         step 4
└── MyAppointments     → AppointmentCard     the list (sibling of the wizard)
```

**Lifted state**, all in `App.jsx`: `booking` (five steps read/write different
fields), `appointments` (the wizard *produces*, the list *displays* — siblings, so it
must live above both), `step`, `session`. `MyAppointments`'s `filter` and
`LoginPanel`'s form fields are deliberately kept **local** as the counter-example.

**Data source.** `Lab2-React/src/api.js` builds the hospital → department → doctor
tree from two live endpoints — `GET /hospitals` (public) and `GET /users/doctors`
(patient-allowed, carries `dept` + `hospitalId`) — then layers a deterministic slot
template on top, because **the backend has no availability model**. Those templates
are display-only; the server remains authoritative (it refuses past dates, duplicate
bookings, slot clashes, and doctors on approved leave). With the API unreachable it
falls back to `src/data/hospitals.js`, a port of `shared/mock-hospitals.js` — the same
API-first-then-fallback posture as `shared/db.js`.

**Running it:**

```bash
cd Lab2-React && npm install
npm run dev      # http://localhost:5173, proxies /api -> :3001
npm run build    # production bundle
npm test         # renders the tree via react-dom/server and asserts on the HTML
```

Sign in with any seed patient (`patient@gmail.com` / `Password123`). It also runs
with the backend down — a banner says which mode you are in.

`Lab2-React/Readme.txt` is the graded submission document: it explains the component
structure and points at exactly where props, callbacks and lifted state are used.
