# NexCare — Project Context

> Working context for anyone (human or AI) picking this repo up cold, mid-task,
> or after a break. **Read this before changing roles, actors, or naming.**
>
> Last full file-by-file sweep: **2026-08-30** (branch `main`, from commit `0c72405`).
> The 2026-08-30 work — doctors as login actors, the hierarchy/visibility scope, and
> the eight-stream revenue model — is described in §4, §5B and §5A. The Lab 2 React
> deliverable was removed from the repo on the same day; see §14.

---

## 0. How to use this file

- §1–§5 are **rules and settled decisions**. Do not contradict them without the
  team agreeing first.
- §6–§13 are a **file-by-file inventory** of what exists and what it does. This is
  the "where was I" map — it is regenerated by sweeping the repo, so if you add or
  remove files, update the matching section.
- §14 is the **cleanup log** — what was broken, what got fixed, and what is still
  deliberately odd. Check it before you report a "bug" or "fix" a misspelt filename.
- §15 has the **commands and gotchas** you need to run anything.

---

## 1. The one rule that governs everything

**NexCare is a strictly NON-CLINICAL platform.**

It digitises hospital *administrative* operations only: appointments and queues, bed
and ward allocation, ambulance coordination, inventory, billing, staff rosters,
feedback. It never touches diagnosis, treatment, prescriptions, or medical records.

Every naming and modelling decision follows from this. When in doubt, ask:
*"is this an administrative action or a clinical one?"* If clinical, it does not
belong in NexCare.

**This rule constrains the FEATURES, not the ACTORS.** Doctors have had a portal
since 2026-08-30, and it is entirely administrative: their own schedule, confirming
and completing bookings, leave requests, and their earnings. Nothing in it records a
diagnosis or a prescription. See §4 — this reverses an earlier decision, deliberately.

Source of truth for scope: `README.md`, `DomainExpertInteraction.md`, `definitions.yml`.

---

## 2. Where things stand right now

**Branch:** `main`, at `0c72405` before the 2026-08-30 work. That work is uncommitted
in the working tree — the team commits it themselves.

**Backend test suite:** 5 suites, 19 tests, **all passing** (`npx jest` in `back-end/`,
~4s). Suites: `array.util`, `id-generator.util`, `hospitals.service`,
`hospital-query.interceptor`, `leave-request.guard`.

**Backend compiles clean.** It did not before 2026-08-28 — `npm run build` emitted
3 TypeScript errors (two wrong middleware import paths, one missing enum member).
Fixed; see §14.

**All seven portals are functional and the authorisation matrix is verified.**
A 48-check scoping script (logins for every role, then the 403/200 matrix in §5B)
passes end to end against a running backend.

**Recent work (last 5 commits, newest first):**

| Commit | What landed |
|---|---|
| `0c72405` | Patient ambulance request was impossible to submit |
| `6288d1c` | The first NexCare revenue model (hospital subscriptions only) |
| `408624c` | Business-logic validations across multiple services |
| `a2726af` | Appointment validations + revenue calculation fix |
| `4410ed1` | Hospital registration + area-based approvals |

**Team branches:** `main`, `vivian`, `nikitha`, `poorna`, `deekshitha`, `Poorvishree`.
Top contributors by commit count: Vivian545 (74), Nikitha101021 (35), poorvishree (35),
Vivianmatheww (19), Deekshitha-1310 (13).

**Open work items:** middleware implementation is the next graded deliverable; the
application it will be evaluated on top of is what §4, §5A and §5B describe.

---

## 3. Canonical actors

These four actors come from the domain expert interaction (31-01-2026) and are the
only ones the SRS recognises:

| Actor | Role value in code | Portal |
|---|---|---|
| Patient | `patient` | `front-end/patient/` |
| Administrative Staff | `administrative_staff` | `front-end/administrative_staff/` |
| Ambulance Staff | `ambulance` | `front-end/ambulance/` |
| Admin | `superuser` | `front-end/superuser/` |

The multi-hospital SaaS upgrade (commit `e6f3fc4`) added two oversight actors on top:

| Actor | Role value in code | Portal |
|---|---|---|
| Regional Officer | `regional_manager` | `front-end/regional-officer/` |
| Hospital Manager | `hospital_manager` | `front-end/hospital_manager/` |

The revised problem statement (2026-08-30) adds a seventh:

| Actor | Role value in code | Portal |
|---|---|---|
| Doctor | `doctor` | `front-end/doctor/` |

### Naming decisions (settled 2026-08-26)

- **"Front Desk" is NOT a role.** It was only ever a display label for
  `administrative_staff`, and it was wrong. The role is displayed as
  **"Administrative Staff"** everywhere. "Front Desk" survives *only* as a
  department value alongside Management and Billing (`superuser/manage-users.js`).
- **The role stays `regional_manager` in code**, displayed as "Regional Officer".
  Do not rename the enum member; the display string is the thing users see.
- **Doctors ARE actors** as of 2026-08-30. Nurses are not — they remain
  directory-only records. See §4.

Role enum source of truth: `back-end/src/common/interfaces/api-response.interface.ts`.

---

## 4. Doctors are login actors (changed 2026-08-30)

**This reverses the previous decision.** Doctors used to be directory-only records
with no portal, on the reasoning that a doctor is a clinical role and NexCare is
non-clinical. The revised problem statement includes doctors, and the reasoning was
wrong anyway: the *role* is clinical, but everything NexCare asks a doctor to do is
administrative. Nothing in the doctor portal records a diagnosis, a treatment or a
prescription.

**The model now:**

- `UserRole.DOCTOR` is a full login actor. `NON_LOGIN_ROLES` in `auth.service.ts`
  contains only `UserRole.NURSE`.
- Doctors self-register at `auth/staff-register.html` (`POST /auth/register-staff`,
  which now accepts `doctor` alongside `administrative_staff` and `ambulance`), or
  are created by the Admin at `superuser/manage-users.html`.
- A registering doctor **must** give a specialisation. That specialisation becomes
  their `dept`, which is the department a patient picks in the booking wizard — a
  doctor without one is unbookable. `AuthService.registerStaff` rejects it.
- A registering doctor's name is normalised to a `Dr. ` prefix
  (`AuthService.doctorDisplayName`), because appointments, leave records and the
  booking wizard all match consultants by name.
- A new doctor is **auto-enrolled on the free listing tier** the moment the account
  is created, so they appear in the revenue model from day one rather than only once
  somebody remembers to place them on a plan.

**Nurses are still directory-only.** `AuthService.login` refuses them with
`Access Denied: 'nurse' is a directory record, not a NexCare login account.` They
exist so rosters, leave records and headcount statistics can reference them.

### The doctor portal — `front-end/doctor/`

| Page | What it does |
|---|---|
| `dashboard.html` | Five KPI tiles (today, awaiting confirmation, completed, patients seen, net earnings), today's schedule with inline Confirm/Complete, and an "up next" list. |
| `appointments.html` | The full list, filtered by status tab and a search box. Confirm and Complete only — cancelling is the patient's or the front desk's call. |
| `earnings.html` | Gross, commission, listing fee, net. A six-month trend. All three tiers priced **at this doctor's own volume**, with a recommendation when another tier is cheaper. Edits the consultation fee. |
| `leaves.html` | Request leave, track it, withdraw a pending one. The hospital manager approves. |
| `profile.html` | Read-only directory entry plus change-password. |

Login at `auth/doctor-login.html`; the combined `auth/login.html` also offers a
Doctor radio. `shared/session.js` has `doctor: '/doctor/'` in `rolePathMap` and a
`case "doctor"` in `redirectByRole`; `shared/nav.js` has a doctor branch.

### `doctorId` on appointments — the attribution fix

An appointment used to carry only `doctor` (a display name). That is not enough to
route a booking to a doctor's portal or attribute consultation commission, so
`doctorId` was added to `CreateAppointmentDto`, the `Appointment` interface, the
service, and `shared/db.js`'s payload whitelist, and the existing 13 rows were
backfilled by name.

`AppointmentsService.findByDoctor` matches on `doctorId` **and falls back to a
normalised name** ("Dr. Sarah Smith", "dr sarah smith" and "Sarah Smith" are one
person), so historical rows are not silently dropped. `RevenueService` uses the same
two-step match. Do not remove the fallback.

### The booking catalogue had drifted — `shared/doctor-directory.js`

The wizard's hospital → department → doctor tree came from `shared/mock-hospitals.js`,
written before the platform had real hospitals or doctor accounts. **Four of its six
hospitals did not exist in the database and 26 of its 32 consultants were not
registered doctors** — so a booking made from it landed on a hospital that could not
be billed and a doctor whose portal would never show it.

`shared/doctor-directory.js` now rebuilds `window.MOCK_HOSPITALS` at page load from
`GET /hospitals` (public) and `GET /users/doctors` (patient-allowed), keeping the real
ids. The one thing it still invents is **availability**: the backend has no roster
model, only leave, so weekdays and slots come from a template seeded deterministically
off the doctor's id. That is display only — the server still refuses past dates,
duplicate bookings, slot clashes and doctors on approved leave. With the API
unreachable the original mock array stands, so the wizard still works offline.

`appointments.js` awaits `window.doctorDirectoryReady` before its first render.

### Where staff leave lives

Both routes now exist and that is deliberate:

- **Doctors** file their own requests at `doctor/leaves.html`.
- **Administrative Staff** record a request on behalf of a staff member at
  `administrative_staff/leave-requests.html`. They **cannot** approve or reject —
  `LeaveRequestGuard.validateLeaveApproval` returns 403.
- **Hospital Manager / Superuser** approve or reject (`PATCH /api/leaves/:id`).

The backend `leaves` module is unchanged and still keys on `doctorId` / `doctorName`.
Its guard blocks overlapping approved leaves and has passing specs — **do not rekey
those fields casually**, you will break `leave-request.guard.spec.ts`.

---

## 5. What the Regional Officer actually does

Role `regional_manager`. Oversight of a *set* of hospitals — one level below the
Admin/superuser, one level above a single hospital's staff.

**Backend powers (enforced by `@Roles`):**

| Endpoint | File |
|---|---|
| `PUT /hospitals/:id` — edit hospital details | `hospitals.controller.ts:65` |
| `GET /hospitals/review-queue` — registrations assigned to them | `hospitals.controller.ts:43` |
| `PATCH /hospitals/:id/regional-review` — clear or reject due diligence | `hospitals.controller.ts:91` |
| `PATCH /hospitals/:id/reject` — routed to `recordRegionalReview(…, 'rejected')` | `hospitals.controller.ts:82` |
| `GET /support-requests` — across their hospitals, filterable to one | `support-requests.controller.ts:11` |
| `PUT /support-requests/:id` — action/resolve a support request | `support-requests.controller.ts:37` |
| file uploads | `uploads.controller.ts:35` |

Explicitly **not** allowed: `PATCH /hospitals/:id/assign-manager` (superuser-only)
and `PATCH /hospitals/:id/verify` (superuser-only).

### The hospital approval chain (two-stage, deliberate)

```
POST /hospitals/register            (public)
   → verificationStatus = pending_verification
     regionalReviewStatus = pending

PATCH /hospitals/:id/assign-manager (superuser)
   → sets assignedManagerId

PATCH /hospitals/:id/regional-review (regional_manager, must be the assigned one)
   → regionalReviewStatus = 'cleared' | 'rejected'
   → NEVER activates the hospital

PATCH /hospitals/:id/verify         (superuser)
   → refuses unless assignedManagerId is set AND regionalReviewStatus === 'cleared'
   → verificationStatus = verified
```

A regional officer clearing a registration is due diligence, not activation.

**Frontend (`front-end/regional-officer/`):**

- `dashboard.html` — four KPI tiles (Assigned Hospitals, Total Doctors, Available
  Beds, Low Stock Items) plus a "Hospitals Under Me" table with occupancy.
- `hierarchy.html` — the officer's own subtree and their visibility scope (§5B).
- `hospital-details.html` — read-only drill-down, five tabs: Overview, Staff &
  Doctors, Beds & Wards, Inventory, Ambulances.
- `hospital-approvals.html` — approve/reject pending hospital registrations.
- `revenue.html` — operational revenue compared across their own hospitals.
- `support-requests.html` — triage requests and advance their status.
- Login at `auth/regional-officer-login.html`.

### Assignment support — Deekshitha's endpoints (merged 2026-08-30)

`UsersService` carries four read-only views that back the Admin's "assign a
regional officer" flow. `superuser/hospital-registrations.js` uses them instead
of filtering the officer list in the browser, so the dropdown shows the same
ranking the backend would produce.

| Endpoint | Returns |
|---|---|
| `GET /users/regional-managers/city/:city` | Officers whose `areas` cover the city |
| `GET /users/regional-managers/:id/workload` | One officer's `RMWorkload` |
| `GET /users/regional-managers/suggest/:city` | Officers ranked: coverage first, then lightest load |
| `GET /users/regional-managers/workloads` | Every officer with their workload |

All four are **superuser-only** and return the standard `ApiResponse` envelope.

Three things were changed on merge, and the reasons matter:

1. **They now use one snapshot per request.** `this.users` and `this.hospitals`
   are getters that re-read and re-parse JSON *on every access*, and
   `suggestRMForHospital` called `getRMWorkload` in a loop — 2 file reads per
   officer, so 7 reads for 3 officers and 101 for 50. It is 2 reads now,
   whatever the officer count.
2. **The envelope is consistent.** `city/:city` returned `ApiResponse`; the other
   three returned raw arrays/objects, and an unknown officer id threw, producing
   a 500 "incident has been logged" instead of a not-found message. Every route
   in this app returns `ApiResponse` — see `response.util.ts`.
3. **Area matching is case-insensitive.** `hospitals.json` stores `"tirupati"`
   for the HSP rows and `"Tirupati"` for H001–H004; an exact-match filter
   silently returned nobody for the lowercase ones.

Workload level is driven by `pendingVerifications * 3 + totalHospitals`, not by
headcount alone: three registrations awaiting a decision is more work than ten
hospitals already verified and quiet. `RevenueService.rollupFor` uses the same
formula deliberately — keep them in step.

### There are three regional officers now, not one

The seed data had a single officer with **no `areas` array**, which meant
`superuser/hospital-registrations.js` — which only offers officers whose `areas`
contain the hospital's city — showed an empty dropdown for every hospital and
nothing could be assigned. Two more officers were added and all three given areas:

| Officer | Areas | Hospitals |
|---|---|---|
| `M001` Rajesh Sharma | Tirupati, Renigunta | H001, H002, H004, HSP001–004, HSP006, HSP008 |
| `M002` Kavitha Menon | Chittoor, Nellore | HSP005, HSP007 |
| `M003` Arjun Raghavan | Chennai | H003 |

`assignedManagerId` on a hospital is the **regional officer**, everywhere it is read
(`users.controller.ts`, `hospitals.service.ts`, `hospital-access.middleware.ts`,
`revenue.controller.ts`, `hierarchy.service.ts`). H001 pointed at `HM001`, a *hospital*
manager — that was a data bug and is fixed. HM001 is linked to H001 through its own
`user.hospitalId`.

With three officers the scope is actually demonstrable: signed in as `M002`, H001 is
neither in the hierarchy nor readable through `/revenue/hospital/H001` (403).

---

## 5A. The revenue model (rebuilt 2026-08-30)

NexCare is a **product**, not one hospital's internal tool. It makes money in seven
ways from three payers, and the streams must never be conflated.

> **Hospital subscriptions were REMOVED on 2026-08-30**, on the course
> instructor's direction, and the reasoning is worth keeping. A subscription is
> *declared*, not *earned*: ₹3.87L of "revenue" existed because
> `hospital-subscriptions.json` said H001 was on the Enterprise plan. Nothing had
> happened, nothing could be pointed at, and one edited line changed the number.
> Everything that remains is triggered by a real event — a payment settled, a
> consultation completed, a booking made — so every rupee traces to a record
> somebody created by using the app. Removing it cut modelled revenue from
> ₹4.27L to ₹38.8K. That is the honest figure.
>
> `subscription-plans.json` and `hospital-subscriptions.json` are deleted, and
> `GET /revenue/plans` and `/revenue/subscriptions` now 404. **Do not
> reintroduce them.** Doctor listing tiers and Care+ memberships stay — those are
> subscriptions people opt into, and they are billed per cycle.

### The three payers

| Payer | Buys | Why they pay |
|---|---|---|
| **Hospitals** | The platform itself | A per-site SaaS licence |
| **Doctors** | Visibility and booking volume | A per-practitioner listing tier |
| **Patients** | Convenience | Per booking, or a membership that waives it |

### The eight streams

| # | Key | Payer | Type | Charged on |
|---|---|---|---|---|
| 1 | `hospital_commission` | hospital | usage | `hospitalCommissionRate` × what the hospital **collected** |
| 2 | `payment_gateway_fee` | hospital | usage | `paymentGatewayRate` × every payment taken through the gateway |
| 4 | `doctor_subscription` | doctor | recurring | Monthly listing fee (the free tier contributes nothing here) |
| 5 | `doctor_commission` | doctor | usage | Tier commission × consultation fee, **only when the appointment completes** |
| 6 | `patient_membership` | patient | recurring | Care+ monthly fee |
| 7 | `patient_booking_fee` | patient | usage | `patientBookingFee` per non-cancelled booking, waived for members |
| 8 | `ambulance_dispatch_fee` | patient | usage | `ambulanceDispatchFee` per completed dispatch, discounted for members |

### The doctor ladder is inverted on purpose

| Tier | Monthly | Commission | Cap |
|---|---|---|---|
| Practice Free | ₹0 | 12% | 25 bookings/month |
| Practice Verified | ₹999 | 8% | unlimited, verified badge |
| Practice Featured | ₹2,499 | 5% | unlimited, top of search |

**The cheaper the subscription, the higher the take rate.** A busy consultant saves
money by moving *up*, so the upgrade is voluntary rather than coerced, and NexCare
earns from every practitioner either way. `doctor/earnings.html` prices all three
tiers **at that doctor's own volume** and says outright when another tier is cheaper —
`RevenueService.getDoctorEarnings` computes `recommendedPlanId` and a reason.

### Patient tiers

| Tier | Monthly | What it does |
|---|---|---|
| Pay as you go | ₹0 | Pays the booking fee each time |
| NexCare Care+ | ₹199 | No booking fee, 20% off ambulance, priority queue |
| Care+ Family | ₹399 | Same for up to 4 people, 25% off ambulance |

Selecting Pay as you go **is** how a patient cancels — there is no separate cancel
endpoint. `patient/membership.html` shows fees waived against membership paid and is
honest when the plan is not paying off.

### Hospitals pay only on what they collect

No plan, no base fee, no bed overage. A single platform-wide
`hospitalCommissionRate` (1.5%) plus the processing fee, both charged when a
payment actually succeeds. A hospital that collects nothing owes nothing.

### Cross-cutting rates — `platform-fee-config.json`

Repriced at runtime by the Admin: `patientBookingFee` (₹39),
`hospitalCommissionRate` (1.5%), `ambulanceDispatchFee` (₹149),
`paymentGatewayRate` (1.9%), `extraStaffSeatFee` (₹250),
`notificationCreditFee` (₹0.35). Rates are stored as **fractions** and edited as
percentages in the UI — `0.019` is 1.9%. `PricingService.updateFeeConfig` refuses a
negative value and a `paymentGatewayRate` above 1.

### Code layout

| File | Owns |
|---|---|
| `revenue/interfaces/revenue.interface.ts` | Hospital plans, subscriptions, the platform/operational roll-ups |
| `revenue/interfaces/pricing.interface.ts` | Doctor and patient plans, fee config, `RevenueStreamLine`, `DoctorEarnings`, `PatientMembership` |
| `revenue/pricing.service.ts` | The **catalogue** — what NexCare charges, and to whom. Seeds every store on first read. |
| `revenue/pricing.module.ts` | Exists so `AuthModule` can use `PricingService` without importing `RevenueModule` (which imports `HospitalsModule`) and closing a cycle. |
| `revenue/revenue.service.ts` | The **computation** — what NexCare earned. Reads the catalogue, never writes to it. |
| `revenue/revenue.controller.ts` | Class-level `@Roles(SUPERUSER)`; every non-Admin route re-declares its own list. |

### Who sees what

| Role | Sees | Where |
|---|---|---|
| Admin (superuser) | All eight streams, unit economics, per-payer split, every plan and rate, and the controls to change them | `superuser/revenue.html` — five tabs |
| Regional Officer | Operational revenue across **their assigned hospitals only** | `regional-officer/revenue.html` |
| Hospital Manager | Their own hospital's collections + what it owes NexCare | Revenue tab in `hospital_manager/dashboard.html` |
| Administrative Staff | Their own hospital's revenue (they raise the bills) | `GET /revenue/hospital/:id` |
| Doctor | Their own earnings, commission, listing fee, and the tier comparison | `doctor/earnings.html` |
| Patient | Their own membership and what it saved them. **Never revenue.** | `patient/membership.html` |

Enforced in `revenue.controller.ts`. `assertMayReadHospital` checks the caller
actually oversees the hospital; `patientKey(user)` maps a login account to its
**patient record id** (`P001`, not `U020`) because appointments and bills are keyed
that way — otherwise a member's own bookings would never match their waiver.

### Revenue and data per regional officer

`GET /revenue/regional-officers` (superuser) answers *"which regions are
carrying the business, and is anyone overloaded?"* — for each officer: hospitals,
pending reviews, workload level, doctors, staff, beds, occupancy, collections,
outstanding, collection rate, platform revenue, share of platform revenue, and a
per-hospital drill-down. Hospitals with **no** officer are reported as a
synthetic `UNASSIGNED` row rather than dropped; an unassigned hospital is a gap
in the review chain, not missing data.

It is computed from **one read of each file**. Bills, staff and beds are bucketed
by hospital once up front (`groupBy`), so a hundredth officer costs a map lookup
rather than another full pass over every bill. Shown as the *Regional officers*
tab in `superuser/revenue.html`, with a click-to-expand hospital breakdown.

## 5C. Payments and the platform ledger (added 2026-08-30)

With subscriptions gone, **every remaining rupee is triggered by a payment or a
completed transaction**, so the payment path is no longer a detail — it is what
the revenue model rests on.

### The simulated gateway — `payments/gateway/mock-gateway.ts`

A SIMULATION. No real card is accepted, contacted or stored; only the last four
digits are ever retained. It behaves like a processor's test mode, so the
outcome is chosen by the card number and **every path can be demonstrated on
demand** — a decline you cannot reproduce is a decline you cannot test.

| Card | Outcome |
|---|---|
| `4242 4242 4242 4242` | approved |
| `4000 0000 0000 0002` | card declined |
| `4000 0000 0000 0069` | expired card |
| `4000 0000 0000 0119` | processing error |
| `4000 0000 0000 9995` | insufficient funds |

Shape checks (Luhn, CVV, expiry) run before the "processor" is reached. An
unrecognised but well-formed card is **declined, not approved** — approving it
would let a demo succeed on a number nobody chose, hiding the simulation.

### The flow

```
POST /payments/intent          amount comes from the BILL, never the client
POST /payments/:id/confirm     gateway authorises
   approved  → bill marked Paid → fee rows written to the ledger
   declined  → bill untouched   → nothing earned
```

`idempotencyKey` makes a replay return the original outcome instead of charging
again — a double-clicked Pay button is the normal way somebody gets billed twice.

### The ledger — `platform-transactions.json`

Every fee NexCare earns is a row, written **at the moment it is charged**, with
the rate stored on the row. This is the point: deriving fees at read time gets
history wrong. Raise the processing rate from 1.9% to 2.2% today and a derived
report silently restates every payment ever taken as though it had been charged
2.2%. Recording the fee when it is taken means repricing changes what happens
**next**, and last month stays what it actually was.

`RevenueService` reads the ledger for `hospital_commission` and
`payment_gateway_fee` and never writes to it — reporting must not be able to
change what was earned. `PaymentsModule` deliberately does not import
`RevenueModule`; the dependency only runs one way.

The 190 rows covering the seeded bills are marked `origin: 'backfill'`; rows from
a real payment are `origin: 'gateway'`.

### Tests — `npx jest`, 8 suites / 51 tests

Revenue had **no coverage at all** before this. Now:

| Suite | Covers |
|---|---|
| `mock-gateway.spec.ts` | 14 tests — every approval and decline path, Luhn/CVV/expiry, last-four-only, determinism |
| `payments.service.spec.ts` | 9 tests — bill settled + both fees recorded, **a decline earns nothing**, idempotent double-click, ownership guards, amount taken from the bill |
| `revenue.service.spec.ts` | 9 tests — exact rupee figures per stream, ledger rate honoured over the current rate, booking fee waived for Care+, doctor commission only on Completed, and an **end-to-end**: one card payment moves the dashboard by exactly ₹600 |

Both service specs redirect `process.cwd()` to a temp dir **before constructing
the services**, because `FileStore` resolves its path at construction time.
Getting that order backwards silently writes to the real seed data.

---

### Nothing is stored pre-aggregated

Every figure is derived at read time from `billing.json`, `appointments.json`,
`ambulance.json` and the six catalogue files, so a total can never drift out of step
with what actually happened. **A bill counts as revenue only when `status === 'Paid'`**
— pending bills are outstanding receivables. **A consultation earns commission only
when the appointment is `Completed`** — a cancelled booking earns nothing.

A doctor with no subscription row is not skipped; `ensureDoctorSubscription`
materialises one on the free tier on first read.

### Seed data was made coherent

The revenue model is only legible if the underlying records agree with each other:

- Appointment `fee` was a flat 100–200 unrelated to what the consultant charges. Every
  appointment now carries its doctor's `consultationFee`.
- Six months of deterministic appointment history (117 rows total) so the trend and
  the per-doctor statements have something to show.
- A **completed consultation now leaves a bill behind** (133 bills, 95 paid). Before
  this, hospital collections came from nine hand-written rows and did not move with
  the appointment history at all.
- Two consultants named in the seed appointments (`Dr. Sarah Smith`, `Dr. Priya
  Sharma`) were not in the directory, so their bookings could not be attributed.
  Sarah Smith was added; the Priya Sharma booking was retargeted to Dr. Priya Nair,
  the real paediatrician at the same hospital — a patient named Priya Sharma already
  exists and a second person with that name would have been confusing.

---

## 5B. The hierarchy and the visibility scope (added 2026-08-30)

**The rule: you see your own node and everything below it — nothing above it,
nothing beside it.**

```
Platform                                       superuser
  └── Region — one per Regional Officer        regional_manager
        └── Hospital                           hospital_manager
              ├── Hospital Manager
              └── Department
                    └── Administrative staff · Doctors · Ambulance crew
```

Patients are **not** org nodes. They are customers of the platform, not staff of a
hospital, so only the Admin sees them in a count.

### The module — `back-end/src/hierarchy/`

Self-contained on purpose: it reads `users.json` and `hospitals.json` directly
through `FileStore` and imports no other feature module, so anything can depend on it
for scoping without risking a circular import.

| Endpoint | Roles | Returns |
|---|---|---|
| `GET /api/hierarchy` | superuser, regional_manager, hospital_manager, administrative_staff | The caller's subtree as nested `HierarchyNode`s |
| `GET /api/hierarchy/scope` | every authenticated role | `VisibilityScope` — level, root, hospital ids, visible roles, counts |

**There is deliberately no `:id` route.** You cannot ask for someone else's subtree;
the caller's is computed from the token. That is what makes the scope an enforcement
point rather than a suggestion.

`HierarchyService` also exposes two helpers other modules can reuse instead of
re-deriving the rule: `hospitalIdsInScope(user)` → `{ ids, all }` and
`canSeeUser(caller, targetUserId)`.

`scope` is open to every role, including the ones with no subtree — a patient or an
ambulance driver gets a truthful "personal scope" answer, which is more useful to the
frontend than a 403.

### The frontend

`shared/hierarchy-view.js` renders both pages: `superuser/hierarchy.html` and
`regional-officer/hierarchy.html` see the *same* view of a *different* subtree. It
**never filters** — if a hospital reaches the browser, the caller was entitled to it.
The tree is nested `<details>` elements, so expand/collapse is the browser's job and
stays keyboard-navigable, and a search box filters by any node in a subtree.

### Verified scoping matrix

Run `bash` against a live backend; all 48 checks pass. Highlights:

| Caller | Route | Expected |
|---|---|---|
| `M002` | `GET /hierarchy` | 200 — 2 hospitals only |
| `M002` | `GET /revenue/hospital/H001` | **403** |
| `M002` | `GET /revenue/hospital/HSP005` | 200 |
| `M001` | `GET /revenue/hospital/HSP005` | **403** |
| `HM001` | `GET /revenue/hospital/H002` | **403** |
| doctor | `GET /appointments/doctor/U006` | **403** |
| doctor | `GET /hierarchy` | **403** (but `/hierarchy/scope` is 200) |
| patient | `GET /revenue/platform/streams` | **403** |

**Read access is scoped, writes are not granted.** `GET /users` filters server-side
to the hospitals assigned to the officer (`users.controller.ts`), so they never see
staff elsewhere. `beds`, `inventory` and `ambulance` grant read via method-level
`@Roles` and are scoped client-side — the same posture `administrative_staff` already
had. Method-level `@Roles` fully overrides the class-level decorator (the guard uses
`getAllAndOverride`), which is how read access was added without granting writes.

---

## 6. Repository map

```
FFSD/
├── back-end/          NestJS 10 API, port 3001, global prefix /api
├── front-end/         Static HTML/CSS/JS, one folder per portal, no framework
├── Database/          ER diagram + MySQL schema (design artefacts, NOT used at runtime)
├── Figma Design/      Figma links
├── videos/            Team video link
├── .claude/           Claude Code local permissions
└── *.md, *.yml, *.pdf Project documentation
```

**The runtime store is JSON files on disk** (`back-end/data/*.json`), not MySQL.
`Database/DBschema.sql` is a design deliverable from an earlier phase and does not
match the running model (it still has `Doctors`, `Treatments`, `Reminders`, `Alerts`
tables and a `MANAGEMENT` role). Do not treat it as the schema of record.

---

## 7. Root-level files

| File | What it is |
|---|---|
| `README.md` | Problem statement, the 4 canonical actors, planned features per actor. The public face of the project. |
| `PROJECT_CONTEXT.md` | This file. |
| `TEST_ACCOUNTS.md` | Seed login reference. **Partly stale** — see §14. |
| `DomainExpertInteraction.md` | 37-min expert interview (31-01-2026, 35 yrs hospital ops experience). Source of the non-clinical constraint, the actor list, and terms like Bed Blocking / Administrative Triage / SPOC. |
| `definitions.yml` | Domain glossary — 21 terms with definition, examples, aliases. Last updated 2026-02-03. |
| `SRS.pdf` | Software Requirements Specification (994 KB). |
| `docs-assembler-config.json` | Doc publishing config → `IIIT-Sricity-FSD-2024-2028/28_NexCare`, branch `master`, GitHub Pages URL. |
| `.gitignore` | Ignores `node_modules/`, `.env`, `*.log`, `back-end/uploads/*`, `back-end/dist/`, `28_NexCare/` (a second clone that used to live here), and `Lab2-React/` (removed 2026-08-30 — see §14). |
| `package-lock.json` | 89 bytes, empty stub at root. Real lockfiles are per-package. |
| `Database/DBschema.sql` | 185-line MySQL DDL — design artefact, not the runtime model. |
| `Database/ER_diagram.png`, `Database/ERDiagram`, `Database/DB Schema.pdf` | ER design artefacts. |
| `Figma Design/FigmaDashboard.md` | Figma design + prototype links. |
| `Figma Design/Figma Designs.pdf` | Exported designs. |
| `videos/video.md` | Google Drive link to the team video. |
| `.vscode/settings.json` | Live Server on port 5501 + docs-assembler syntax colours. |
| `.claude/settings.local.json` | Claude Code allowlist for this repo. |

---

## 8. Backend architecture

**Stack:** NestJS 10, Express, TypeScript 5, `class-validator`/`class-transformer`,
`@nestjs/swagger`, `helmet`. No database driver — persistence is JSON files.

**Boot:** `src/main.ts`
1. Loads `.env` by hand (no `dotenv` dependency).
2. Warns if `JWT_SECRET` is missing or left at the shipped default.
3. CORS — permissive by default (reflect any origin) so the WSL/multi-host dev setup
   works; set `CORS_ORIGIN` to a comma-separated allowlist to lock down.
4. Global prefix `api`.
5. Body limit `MAX_BODY_BYTES` (default 1 MB) on json + urlencoded.
6. Global `ValidationPipe` — `whitelist`, `forbidNonWhitelisted`, `transform`,
   implicit conversion, custom `exceptionFactory` producing `{field, messages[]}`.
7. Global `AllExceptionsFilter`.
8. Swagger built and **written to `docs/swagger.json` on every boot**, served at
   `/api/docs`.
9. `app.init()`, then Express-level `notFoundMiddleware` + `errorHandlerMiddleware`
   (these catch what Nest's filter cannot: body-parser failures, middleware throws,
   unmatched routes).
10. Listens on `0.0.0.0:${PORT||3001}` and prints every reachable LAN URL.
11. `uncaughtException` / `unhandledRejection` / SIGINT / SIGTERM all flush the file
    logger before exiting.

### Request pipeline (order matters)

```
SecurityMiddleware        headers, rate limiting, payload size     app-level, forRoutes('*')
RequestLoggerMiddleware   request id, access + error log lines     app-level, forRoutes('*')
  ↓
AuthGuard    (APP_GUARD)  @Public() bypasses; else verify Bearer JWT
                          → request.user = {id,email,role,name,patientId,hospitalId}
RolesGuard   (APP_GUARD)  no @Roles ⇒ any authenticated user;
                          else user.role must be in the list (getAllAndOverride,
                          so a method-level @Roles fully replaces the class-level one)
  ↓
Route-scoped middleware   BedStatusChangeMiddleware  (beds status/allocate/release/PUT/PATCH)
                          HospitalAccessMiddleware   (hospitals/:id family)
                          FileUploadMiddleware       (POST /uploads)
  ↓
Interceptors              HospitalQueryInterceptor   (GET /hospitals, /hospitals/nearby)
                          InventoryAuditInterceptor  (PATCH /inventory/:id/restock, /use)
                          FileInterceptor('file')    (POST /uploads)
  ↓
ValidationPipe → Controller → Service → FileStore(JSON)
```

### Auth

Hand-rolled JWT (HMAC via node `crypto`, no `jsonwebtoken` dependency).
Payload: `{sub, email, role, name, patientId, hospitalId, iat, exp}`.
Passwords: scrypt, stored as `scrypt$<saltHex>$<hashHex>`, compared with
`timingSafeEqual`. Legacy plaintext seed passwords are upgraded in place on first
login (see the gotcha in §15).

Login rejects, in order: unknown email → wrong password → role mismatch against the
role the form submitted → directory-only role (`doctor`/`nurse`) → inactive account.

---

## 9. Backend file inventory

### Feature modules — each is `X.module.ts` + `X.controller.ts` + `X.service.ts` (+ `dto/`, `interfaces/`)

| Module | Service LOC | Notes |
|---|---|---|
| `auth/` | 19 KB | JWT sign/verify, scrypt hashing, login, patient register, staff register, sessions, change-password. |
| `users/` | 10 KB | User CRUD; `GET /users` is hospital-scoped for `regional_manager`. `GET /users/doctors` feeds the booking wizard. |
| `patients/` | 13 KB | Patient records, search, blood-group and age-range queries. |
| `appointments/` | 20 KB | Booking with past-date, duplicate, slot-conflict and doctor-on-leave checks. Confirm/complete/cancel. Revenue = completed − cancelled. |
| `billing/` | 15 KB | Bills with CGST/SGST, payments, overdue, revenue. |
| `ambulance/` | 15 KB | Requests, dispatch, status ladder, per-patient and per-crew views. |
| `feedback/` | 12 KB | Ratings, categories, unresolved and high-priority views, status changes. |
| `beds/` | 12 KB | Bed CRUD, allocate/release/status, ward and occupancy stats. Has `middleware/bed-status-change.middleware.ts`. |
| `inventory/` | 19 KB | Stock, restock/use, low-stock and out-of-stock, per-item audit trail. Has `interceptors/inventory-audit.interceptor.ts`. |
| `hospitals/` | 10 KB | Registration, public search, nearby (proximity score pincode 3 > city 2 > state 1), two-stage approval. **Two controllers** — `hospitals.controller.ts` and `hospital-details.controller.ts` (the Regional Officer drill-down tabs, kept separate so other members' files stay untouched). Has `hospital-access.middleware.ts` + `hospital-query.interceptor.ts`. |
| `system/` | 12 KB | Activity/audit log + system settings. Every other service logs through it. |
| `leaves/` | 8 KB | Leave records keyed on `doctorId`/`doctorName`. `guards/leave-request.guard.ts` blocks overlapping approved leaves and restricts approval to hospital_manager/superuser. |
| `support-requests/` | 3 KB | Hospital→region escalation. Role-scoped read in the controller itself, not by `@Roles`. |
| `uploads/` | 5 KB | Multer file upload, download, delete. |
| `revenue/` | 26 KB | Both roll-ups plus the eight-stream income statement, doctor earnings and patient membership. `pricing.service.ts` owns the catalogue; `pricing.module.ts` exists to keep `AuthModule` out of an import cycle. See §5A. |
| `hierarchy/` | 12 KB | The org tree and the visibility scope. Reads the JSON stores directly and imports no other feature module, so anything may depend on it. See §5B. |
| `departments/`, `wards/`, `equipment/` | ~1 KB each | Read-only lookup lists, `hospitalId`-filterable. |

### Shared/common

| Path | Purpose |
|---|---|
| `common/interfaces/api-response.interface.ts` | `ApiResponse<T>` + **every status enum** and `UserRole`. The single source of truth for roles. |
| `common/guards/auth.guard.ts` | Bearer JWT → `request.user`. Honours `@Public()`. |
| `common/guards/roles.guard.ts` | `@Roles()` check via `getAllAndOverride`. |
| `common/decorators/public.decorator.ts` | `@Public()` — skips both guards. |
| `common/decorators/roles.decorator.ts` | `@Roles(...)`. |
| `common/utils/response.util.ts` | `ResponseUtil.success/created/updated/error/notFound/forbidden/serverError`. **Every service depends on this — do not change its shape.** |
| `common/utils/file-store.util.ts` | `FileStore<T>` — load/save `data/<file>.json`, seeds on first access. The whole persistence layer. |
| `common/utils/id-generator.util.ts` (+ spec) | ID and token generation. |
| `common/utils/array.util.ts` (+ spec) | Array helpers. |
| `common/utils/sanitizer.util.ts` | Input sanitising. |
| `common/filters/all-exceptions.filter.ts` | Global catch → standard envelope + error log. |
| `common/filters/http-exception.filter.ts` | Narrower HTTP filter. |
| `common/logging/file-logger.ts` | Buffered writer for `logs/access-*.log`, `app-*.log`, `error-*.log`. |
| `common/logging/logs.controller.ts` | `GET /logs`, `GET /logs/files` (superuser + administrative_staff). |
| `common/validation/*`, `common/services/validation.service.ts` | DTO validator helper, frontend validation service, worked examples. |
| `common/dto/` | `date-range.dto.ts`, `query-params.dto.ts`. |
| `lodger.middleware.ts` | **Misspelt on purpose — it is the real file.** Exports `RequestLoggerMiddleware` (aliased `LoggerMiddleware`, `LodgerMiddleware`), `SecurityMiddleware`, `FileUploadMiddleware`, `MAX_UPLOAD_BYTES` (5 MB), `errorHandlerMiddleware`, `notFoundMiddleware`. |
| `logger.middleware.ts` | One line: `export * from './lodger.middleware';` |
| `common/middleware/*.ts` | Three one-line re-export shims to the same file. |
| `src/dto/index.ts` + `src/dto/README.md` | Barrel export + DTO conventions doc. |

### Non-source backend files

| Path | Notes |
|---|---|
| `back-end/data/*.json` | **The database.** 18 files — see §11. |
| `back-end/docs/swagger.json` | 134 KB, regenerated on every server boot. Expect it dirty in `git status` after running the backend. |
| `back-end/logs/` | `README.md` is tracked; the `.log` files are gitignored. |
| `back-end/uploads/` | `.gitkeep` only; uploads gitignored. |
| `back-end/generate-swagger.ts` | Standalone swagger dump. **Prefer just booting the server** — this prints to stdout mixed with Nest logs and emits the same 106 paths in a different key order (huge meaningless diff). |
| `back-end/README.md` | Original module/endpoint doc from the scaffold phase. |
| `back-end/TEAM_HANDOFF.md` | Historical per-member work split (Deekshitha scaffold; Vivian RBAC+integration; Nikitha data+testing; Poornasri DTO+Swagger; Poorvishree ER+service logic). Mostly done — read as history, not as a todo list. |
| `back-end/dist/` | Gitignored and no longer tracked (4 stray files were untracked on 2026-08-28). Regenerate with `npm run build`. |

---

## 10. Complete route map

Every route is under the global prefix **`/api`**. "class" = the controller-level
`@Roles`; a method row that lists roles **replaces** the class list entirely.

### `auth` — no class-level roles
| Method | Path | Roles |
|---|---|---|
| POST | `/auth/login` | **Public** |
| POST | `/auth/register` | **Public** (patients only) |
| POST | `/auth/register-staff` | **Public** (administrative_staff, ambulance, **doctor**) |
| POST | `/auth/logout/:userId` | any authenticated |
| GET | `/auth/current/:userId` | any authenticated |
| GET | `/auth/sessions` | superuser |
| PATCH | `/auth/change-password` | any authenticated |

### `users` — class: superuser, administrative_staff
| Method | Path | Roles |
|---|---|---|
| GET | `/users` | + regional_manager (server-side hospital scoping) |
| GET | `/users/doctors` | superuser, administrative_staff, **patient** |
| POST · PUT · PATCH · DELETE | `/users`, `/users/:id`, `/users/:id/status` | class |
| GET | `/users/stats/overview`, `/users/role/:role`, `/users/search/:query`, `/users/:id` | class |

### `patients` — class: superuser, administrative_staff
| Method | Path | Roles |
|---|---|---|
| GET | `/patients`, `/patients/stats/overview`, `/patients/search/:query`, `/patients/blood-group/:bloodGroup`, `/patients/age-range` | class |
| POST | `/patients` | class |
| GET · PUT · PATCH | `/patients/:id` | + **patient** |
| DELETE | `/patients/:id` · PATCH `/patients/:id/status` | class |

### `appointments` — class: superuser, administrative_staff, doctor
| Method | Path | Roles |
|---|---|---|
| GET | `/appointments` | + **patient** |
| POST | `/appointments` | superuser, administrative_staff, **patient** |
| GET | `/appointments/doctor/:doctorId`, `/doctor/:doctorId/stats` | superuser, administrative_staff, hospital_manager, **doctor** — a doctor must pass `me`; any other id is 403 |
| GET | `/appointments/patient/:patientId`, `/appointments/:id` | + **patient** |
| GET | `/appointments/stats/overview`, `/department/:department`, `/today` | class |
| PUT · PATCH · DELETE | `/appointments/:id` | class |
| PATCH | `/appointments/:id/confirm`, `/complete` | class — a **doctor** may only action an appointment on their own list (`assertDoctorOwnsAppointment`) |
| PATCH | `/appointments/:id/cancel` | superuser, administrative_staff, **patient** |

### `billing` — class: superuser, administrative_staff
`GET /billing`, `GET /billing/patient/:patientId`, `GET /billing/:id`,
`PATCH /billing/:id/pay` also allow **patient**.
Others (`POST`, `PUT`, `PATCH`, `DELETE`, `/stats/overview`, `/overdue`, `/revenue`) are class-only.

### `ambulance` — class: superuser, administrative_staff, ambulance
| Method | Path | Roles |
|---|---|---|
| GET | `/ambulance` | + **patient**, **regional_manager** |
| POST · DELETE | `/ambulance`, `/ambulance/:id` | + **patient** |
| GET | `/ambulance/patient/:patientId`, `/ambulance/:id` | + **patient** |
| GET | `/ambulance/stats/overview`, `/active`, `/assigned/:assignedTo` | class |
| PUT · PATCH | `/ambulance/:id`, `/:id/dispatch`, `/:id/complete`, `/:id/status` | class |

### `feedback` — class: superuser, administrative_staff
`GET /feedback`, `POST /feedback`, `GET /feedback/patient/:patientId` also allow **patient**.
`/stats/overview`, `/category/:category`, `/rating/:rating`, `/unresolved`,
`/high-priority`, `/:id` (GET/PUT/PATCH/DELETE), `/:id/status` are class-only.

### `beds` — class: superuser, administrative_staff
`GET /beds` also allows **regional_manager**. Everything else class-only:
`POST`, `/stats/overview`, `/ward/:ward`, `/available`, `/patient/:patient`,
`/occupancy`, `/:id` (GET/PUT/PATCH/DELETE), `/:id/allocate`, `/:id/release`, `/:id/status`.

### `inventory` — class: superuser, administrative_staff
`GET /inventory` also allows **regional_manager**. Everything else class-only:
`POST`, `/stats/overview`, `/low-stock`, `/out-of-stock`, `/category/:category`,
`/location/:location`, `/search/:query`, `/audit/:itemId`, `/:id` (GET/PUT/PATCH/DELETE),
`/:id/restock`, `/:id/use` (last two audited).

### `hospitals` — `HospitalsController`, no class-level roles
| Method | Path | Roles |
|---|---|---|
| GET | `/hospitals` | **Public** (+ HospitalQueryInterceptor) |
| GET | `/hospitals/nearby` | **Public** (+ HospitalQueryInterceptor) |
| GET | `/hospitals/:id` | **Public** |
| POST | `/hospitals/register` | **Public** |
| GET | `/hospitals/review-queue` | regional_manager |
| PUT | `/hospitals/:id` | superuser, regional_manager, hospital_manager |
| PATCH | `/hospitals/:id/verify` | superuser (gated on regional clearance) |
| PATCH | `/hospitals/:id/reject` | superuser, regional_manager |
| PATCH | `/hospitals/:id/regional-review` | regional_manager |
| PATCH | `/hospitals/:id/assign-manager` | superuser |

### `hospitals` — `HospitalDetailsController` — class: superuser, regional_manager, hospital_manager, administrative_staff
`GET /hospitals/:id/doctors`, `/beds`, `/inventory`, `/ambulances`.

### `support-requests` — no class-level roles, scoped in code
| Method | Path | Behaviour |
|---|---|---|
| GET | `/support-requests` | superuser → any; regional_manager → their hospitals (filterable); hospital_manager → **locked to own hospital, client `hospitalId` ignored**; staff → own hospital |
| POST | `/support-requests` | any authenticated; `hospitalId` defaults to the caller's |
| PUT | `/support-requests/:id` | superuser, regional_manager, hospital_manager |

### `revenue` — class: superuser
Platform routes inherit the class list; everything else re-declares its own.

| Method | Path | Roles |
|---|---|---|
| GET | `/revenue/platform/overview`, `/platform/trend`, `/platform/streams` | class |
| GET | `/revenue/regional-officers` | class — per-officer revenue + operational data |
| GET · PATCH | `/revenue/fees` | class |
| PATCH | `/revenue/doctor-plans/:id`, `/patient-plans/:id` | class |
| GET | `/revenue/doctor-subscriptions`, `/patient-subscriptions` | class |
| GET | `/revenue/doctor-plans` | + doctor, hospital_manager, administrative_staff |
| GET | `/revenue/patient-plans` | + patient, administrative_staff |
| GET | `/revenue/hospital/:hospitalId` | superuser, regional_manager, hospital_manager, administrative_staff (+ `assertMayReadHospital`) |
| GET | `/revenue/my-hospitals/compare` | superuser, regional_manager |
| GET · PATCH | `/revenue/doctor/me`, `/doctor/me/subscription` | **doctor** (own only, from the token) |
| GET | `/revenue/doctor/:doctorId` | superuser, hospital_manager, regional_manager |
| PATCH | `/revenue/doctor-subscriptions/:doctorId` | superuser, hospital_manager, regional_manager |
| GET · PATCH | `/revenue/patient/me/membership` | **patient** (own only, keyed on `patientId`) |

Route order matters: `doctor/me` is declared **before** `doctor/:doctorId`.

### `users` — regional manager assignment support — superuser only
`GET /users/regional-managers/city/:city`, `/:id/workload`, `/suggest/:city`,
`/workloads`. See §5.

### `payments` — class: superuser, administrative_staff, patient
| Method | Path | Roles |
|---|---|---|
| GET | `/payments/test-cards` | class |
| POST | `/payments/intent`, `/payments/:id/confirm` | class (patients: own bills only) |
| GET | `/payments` | class (patients: own only) |
| GET | `/payments/ledger` | **superuser only** |

### `hierarchy` — class: superuser, regional_manager, hospital_manager, administrative_staff
| Method | Path | Roles |
|---|---|---|
| GET | `/hierarchy` | class |
| GET | `/hierarchy/scope` | **every authenticated role**, including doctor, ambulance and patient |

No `:id` route exists — see §5B.

### `leaves` — class: superuser, hospital_manager, administrative_staff, doctor
`GET /leaves`, `/leaves/calendar`, `/leaves/:id`, `POST /leaves`,
`PATCH /leaves/:id`, `DELETE /leaves/:id`.
`LeaveRequestGuard` narrows POST (no overlapping approved leave) and PATCH
(approve/reject is hospital_manager + superuser only — administrative_staff gets 403).

### `system` — class: superuser, administrative_staff
`GET /system/activity`, `POST /system/activity`, `GET /system/settings`,
`/settings/key/:key`, `/stats`, `/activity/date-range`, `/activity/user/:userId`,
`/activity/recent`, `/settings/category/:category`, `/activity/search/:query`,
`/activity/:id`, `/settings/:id`, `PUT /system/settings`, `PUT /system/settings/:id`.

### `uploads` — class: superuser, administrative_staff, hospital_manager, regional_manager
`POST /uploads` (FileInterceptor, 5 MB cap), `GET /uploads`, `GET /uploads/:id`,
`GET /uploads/:id/download`, `DELETE /uploads/:id` (**superuser + administrative_staff only**).

### `logs` — class: superuser, administrative_staff
`GET /logs`, `GET /logs/files`.

### lookups — class: superuser, administrative_staff, regional_manager
`GET /departments`, `GET /wards`, `GET /equipment` — all accept `?hospitalId=`.

---

## 11. The data store (`back-end/data/`)

Every service persists through `FileStore<T>`, so **all 25 files survive a restart**.

| File | Rows | Key fields |
|---|---|---|
| `users.json` | 45 | `id, name, email, role, status, password` (+ `hospitalId, patientId, dept, city, state, pincode` where relevant) |
| `patients.json` | 15 | `id, fullName, phone, email, patientIdDisplay, memberSince, status, bloodGroup, age` |
| `appointments.json` | 13 | `id, patientId, patientName, department, doctor, dateLabel, timeLabel, token, fee, status, reason` |
| `hospitals.json` | 12 | 36 fields incl. `verificationStatus, regionalReviewStatus, assignedManagerId, specialities, totalBeds, availableBeds, lat/long` |
| `beds.json` | 75 | `id, ward, status, patient, hospitalId` |
| `billing.json` | 9 | `id, patientId, subtotal, cgstRate, sgstRate, total, items[], payments[]` |
| `inventory.json` | 36 | `id, name, category, quantity, minStock, unit, location, status, hospitalId` |
| `inventory-audit.json` | 7 | `id, itemId, action, quantityBefore, quantityAfter, userId, timestamp, notes` |
| `ambulance.json` | 5 | `id, patientId, pickupLocation, contact, status, assignedTo, hospitalId` |
| `feedback.json` | 5 | `id, patientId, sender, type, category, subject, summary, rating, status` |
| `leaves.json` | 4 | `id, doctorId, doctorName, hospitalId, startDate, endDate, reason, status, approvedBy` |
| `support-requests.json` | 5 | `id, hospitalId, createdBy, assignedManagerId, category, subject, priority, status` |
| `system-activity.json` | 53 | `id, timestamp, userId, action, details, module, severity` |
| `system-settings.json` | 5 | `id, key, value, description, category` |
| `departments.json` | 17 | `id, name, hospitalId` |
| `wards.json` | 19 | `id, name, hospitalId` |
| `equipment.json` | 20 | `id, name, type, status, hospitalId` |
| `uploads.json` | 0 | populated at runtime |
| `platform-transactions.json` | 192 | **The platform earnings ledger.** One row per fee charged — see §5C |
| `payment-intents.json` | runtime | Payment attempts, including declines |
| `doctor-plans.json` | 3 | Practice Free / Verified / Featured — `monthlyFee, commissionRate, monthlyBookingCap` |
| `doctor-subscriptions.json` | 20 | `doctorId, planId, status, consultationFee` — one per doctor |
| `patient-plans.json` | 3 | Pay as you go / Care+ / Care+ Family |
| `patient-subscriptions.json` | 5 | `patientId, planId, status, renewsOn` |
| `platform-fee-config.json` | 1 | Single-row store: booking, ambulance, gateway, seat and notification rates |

### Actual seed roster (verified 2026-08-28 — supersedes `TEST_ACCOUNTS.md`)

48 users, **all of which can log in**: 1 superuser, **3 regional_manager**,
1 hospital_manager, 5 administrative_staff, 3 ambulance, 15 patients, **20 doctors**.
Nurses are still directory-only but the seed ships none.

Also 117 appointments (79 completed), 133 bills (95 paid) — see §5A on why the seed
data was made coherent.

Login accounts worth knowing — password `Password123` for all:

| Role | Name | Email | Hospital |
|---|---|---|---|
| superuser | Rajesh Kumar | `superuser@nexcare.com` | — |
| regional_manager (`M001`) | Rajesh Sharma | `regional@nexcare.com` | Tirupati + Renigunta |
| regional_manager (`M002`) | Kavitha Menon | `regional2@nexcare.com` | Chittoor + Nellore |
| regional_manager (`M003`) | Arjun Raghavan | `regional3@nexcare.com` | Chennai |
| doctor (`U005`) | Dr. Sunita Sharma | `sunita@nexcare.com` | H001 |
| hospital_manager (`HM001`) | Srinivas Rao | `hospitalmanager@nexcare.com` | H001 |
| administrative_staff | Priya Reddy | `admin@nexcare.com` | H001 |
| administrative_staff | Lakshmi Menon | `lakshmi@nexcare.com` | H002 |
| administrative_staff | Divya Krishnan / Karthik Raman | `divya@` / `karthik@nexcare.com` | H003 |
| administrative_staff | Anita Joshi | `anita@nexcare.com` | H001 |
| ambulance | Alex Martinez / Suresh Babu / Manoj Selvam | `ambulance@` / `suresh@` / `manoj@nexcare.com` | H001 / H002 / H003 |
| patient | John Anderson | `patient@gmail.com` | — |
| patient | +14 more (`patient2..8@gmail.com`, `ananya.sharma@`, `rahul.verma@`, …) | | |

**Hospitals:** H001 NexCare AIIMS Super Speciality (Tirupati, mgr HM001) ·
H002 Apollo Health City (Tirupati, mgr M001) · H003 Fortis Care (Chennai, mgr M001) ·
H004 Sri Venkateswara Care Center (Tirupati, no mgr) · HSP001–HSP008 (Tirupati,
Chittoor, Renigunta, Nellore — all verified, no manager).

**Login pages:** `auth/superuser-login.html` · `auth/regional-officer-login.html` ·
`auth/staff-login.html` (administrative_staff + ambulance) · `auth/doctor-login.html` ·
`auth/hospital-manager-login.html` · `auth/patient-login.html` ·
`auth/login.html` (combined, now including Doctor).

---

## 12. Frontend architecture

Plain HTML + CSS + vanilla JS, served statically (`npx serve` on 8080, or VS Code
Live Server on 5501). No build step, no bundler, no framework.

### The shared layer — `front-end/shared/`

| File | Size | Role |
|---|---|---|
| `api.js` | 21 KB | **The API client.** `NexCareAPI` class + 13 modules (`Auth`, `Users`, `Patients`, `Appointments`, `Billing`, `Ambulance`, `Feedback`, `Beds`, `Inventory`, `Hospitals`, `Leaves`, `SupportRequests`, `System`) exposed on `window.NexCareAPI`, plus legacy flat aliases. Resolves the backend URL as `http://<current hostname>:3001/api` so it works on localhost, LAN IP, or WSL IP. Normalises every response to `{success, data, message}`. Also exports `window.pageLink(page, params)` — builds in-app links that keep `.html` only when the current URL has it, because static hosts 301 `/page.html?x=1` → `/page` and **drop the query string**. |
| `session.js` | 12 KB | **Auth + routing guard.** Hides `<html>` before paint, decodes the JWT client-side (expiry only — signature is the backend's job), redirects unauthenticated users off protected pages, enforces `rolePathMap` so a role cannot sit in another portal, fades the page in, injects the floating back button. Exports `loginUser`, `redirectByRole`, `logoutUser`. |
| `db.js` | 35 KB | **API-first compatibility bridge.** `NexCareDB` / `NexCareStore` try the API and fall back to `localStorage` (`nexcare_db_v3`) with a small seed. This is why the portals still work with the backend down. |
| `nav.js` | 11 KB | Injects the sidebar and the role display name. **Only the superuser and regional-officer portals actually load it** — see §14. |
| `ui-components.js` | 11 KB | `NexCareUI` — success modal, toast, confirm. `api.js` calls `NexCareUI.showToast` on network errors. |
| `mock-hospitals.js` | 30 KB | `window.MOCK_HOSPITALS` — hospital → departments → doctors → `availableDays` → `slots`. **The booking wizard's step 0–2 data source.** |
| `mockdb.json` | 7 KB | Legacy seed JSON. |
| `doctor-directory.js` | 6 KB | Rebuilds `window.MOCK_HOSPITALS` from the live `/hospitals` and `/users/doctors` before the wizard renders, keeping the real ids, and layers a deterministic availability template on top. Falls back to the offline catalogue. **See §4 — without this, bookings reach no real doctor or hospital.** |
| `hierarchy-view.js` | 6 KB | Shared renderer for both hierarchy pages. Draws whatever the backend hands it and never filters. |
| `portal.css` | 6 KB | The header/page-body/KPI/panel/table/pill chrome the superuser and regional-officer pages each re-declared inline. Load after `global.css` and `nav.css`. |
| `portal.js` | 4 KB | `currentUser`, `money`, `percent`, `esc`, `setText`, `setHTML`, `initials`, `fillHeader`, `notify`, `todayLabel`, `appointmentTime`. Plain globals, not a module — the portals use `<script src>`, not `import`. |
| `global.css`, `nav.css` | 11 + 3 KB | Base styles and sidebar styles. |

`front-end/logo.js` (85 lines) defines the `<nex-care-logo>` custom element with
Shadow DOM so its styles never leak.

### Portals

| Portal | Pages | Sidebar | Notes |
|---|---|---|---|
| `patient/` | dashboard, appointments, billing, ambulance, feedback, membership, profile, hospital-search | hardcoded in HTML | Biggest CSS (`styles.css` 58 KB). `dataStore.js` is a thin ES-module proxy to `window.NexCareStore`. The 3 legacy `book-appointment-step*.html` pages were removed on 2026-08-28. |
| `administrative_staff/` | dashboard, bed-allocation, inventory, manage_appointments, patient-directory, patient_checkin, staff_scheduling, generate-bill, feedback, leave-requests, system-logs | hardcoded in HTML | Each page has a matching `.js`; most define their own local `apiGet`/`apiRequest` rather than using `shared/api.js`. |
| `superuser/` | dashboard, hierarchy, hospital-registrations, patient-directory, manage-users, system-settings, feedback, revenue, reports | **`shared/nav.js`** | `reports.js` is the largest (594 lines). `revenue.html` now has five tabs — All streams, Hospitals, Doctors, Patients, Pricing controls. |
| `regional-officer/` | dashboard, hierarchy, hospital-approvals, hospital-details, revenue, support-requests | **`shared/nav.js`** | `hospital-details.html` has the 5 read-only tabs. |
| `doctor/` | dashboard, appointments, earnings, leaves, profile | **`shared/nav.js`** | Built on `shared/portal.css` + `portal.js`. See §4. |
| `hospital_manager/` | dashboard | hardcoded | 483-line tabbed dashboard: overview, leaves (approve/reject), staff, support. |
| `ambulance/` | index.html | hardcoded | Single-page app: 6 `data-page` views (dashboard, ambulance-requests, assigned-dispatch, active-transport, completed-transports, profile). `app.js` is **3522 lines / 135 KB** — the largest file in the repo. |
| `auth/` | login, patient-login, patient-register, staff-login, staff-register, superuser-login, regional-officer-login, hospital-manager-login, signup | n/a | |
| `landing/` | landing.html (64 KB, sections: home, features, roles, how-it-works, reviews, contact), hospital-registration.html | n/a | `hospital-registration.html` is now the **only** hospital sign-up page; `auth/login.html` links here. |

### The patient booking wizard (the React-conversion candidate)

`patient/appointments/appointments.html` is one page with three top-level sections
toggled by `display`: `#appointmentLanding`, `#myAppointments`, `#bookingFlow`.
`appointments.js` (1069 lines) drives it with two module-level variables:

```js
let bookingData = { hospital, department, doctorId, doctor, date, time, patientInfo };
let currentStep = 0..4;
```

`renderBookingStep()` dispatches to `renderStep0` (hospital) → `renderStep1`
(department) → `renderStep2` (date & doctor) → `renderStep3` (details) →
`renderConfirmation`. `getBookedSlotsForDoctor()` unions booked slots from three
sources (API → `NexCareStore` → raw `localStorage`) to grey out taken times.

That shared `bookingData` is exactly the "lift state up to the common parent" the lab
asks for, and each step becomes a child component receiving props + an `onSelect`
callback.

---

## 13. Frontend file counts

29 files in `administrative_staff/` · 18 in `patient/` (+2 in `patient/appointments/`) ·
17 in `superuser/` · 12 in `auth/` · 13 in `shared/` · 10 in `regional-officer/` ·
10 in `doctor/` · 4 in `landing/` · 4 in `ambulance/` · 3 in `hospital_manager/` ·
2 in `assets/`.

**Every relative `href`/`src` across all 54 HTML pages resolves** (re-audited
2026-08-30; the only non-resolving match is a `tel:` link on the landing page).
Re-run the audit after adding pages.

Also: `front-end/debug.html` (5 KB dev scratch page), `front-end/jsconfig.json`
(`checkJs: false`), `front-end/package-lock.json` (88-byte stub),
`front-end/administrative_staff/generate_pages.js` (257 lines, page-generation helper
loaded by 0 pages — still dead, left in place deliberately).

---

## 14. Cleanup log and remaining traps

### Done on 2026-08-31 (fourth pass — the `main` bug audit)

Worked from `NexCare_Main_Bug_Audit_2026-08-31.md`, which audited the 19 commits
`b203c14..74ea0c9`. `main` did not compile (48 errors) and did not boot. All 13
audited defects are fixed, plus one the audit missed. Build clean, 10 unit
suites / 55 tests pass, E2E 7/7.

**Build and boot (N1, P1).** `ApiResponse` was used but never imported in six
controllers — payments, schedules, hierarchy, wards, equipment, departments.
Decorators evaluate at module load, so this was a startup `ReferenceError`, not
just a type error. `pdfkit` had landed in `package.json` without anyone being
told; `npm install` in `back-end/` is required after pulling.

**`npm run test:e2e` no longer destroys the data directory (P7).** It reseeds,
which overwrites all 11 files in `back-end/data/`. `test/global-setup.js` now
snapshots the directory to `back-end/.e2e-data-backup/` and
`test/global-teardown.js` restores it. A snapshot left behind by an interrupted
run is restored, not overwritten, on the next run.

**The seed now agrees with the type system (N2, N3, N4).**
`scripts/generate-comprehensive-seed.js` mirrors the `BedStatus`,
`AppointmentStatus`, `BillStatus` and `AmbulanceStatus` enums and asserts the
mirrors against the actual `.ts` source before writing anything. It also
validates every generated record. **A drift now fails the seed loudly** — that
single missing check was the root cause behind six of the audited defects.

Specifically: bed statuses were `OCCUPIED`/`AVAILABLE` against a lowercase enum,
so *every* bed allocation and discharge failed and every bed statistic read 0;
beds carried `wardId`/`wardName`/`patientId` where the `Bed` interface declares
`ward`/`patient`, so ward rollups all counted under `"undefined"` and the
duplicate-allocation guard was dead; appointments used `BOOKED` (not an enum
member) and uppercase variants, so cancelled slots blocked rebooking. Bed IDs
also had a double hyphen (`BED-H001--01-001`) from `wardId.slice(-3)`.

`beds.service.ts` additionally normalises on read, so records persisted by older
builds still work — fixing the seed alone would have left them broken.

**Not in the audit: `data/ambulance.json` held the wrong entity.** It is the
`AmbulanceRequest` collection, but the seed filled it with 22 *vehicle* records
— no `patientId`, no `pickupLocation`, and status `Available`, which is not an
`AmbulanceStatus`. Patient ambulance history, the dispatch queue and every
ambulance statistic read empty. It now seeds real requests, with the fleet
details (`vehicleNumber`, `driverName`) riding on the dispatched ones, which is
what the dispatch DTOs already expected.

**Silently-wrong financial output (P2, P3, P4).** `health-score.service.ts` read
`bills.json` (the file is `billing.json`), `b.date` (bills carry `visitDate`)
and `a.date` (appointments carry `dateLabel`) — `new Date(undefined)` is an
Invalid Date that fails every comparison, so revenue and the 30-day windows were
permanently empty and every hospital ate the low-utilisation penalty forever.
`bill-pdf.service.ts` printed "Date: Invalid Date" and recomputed the subtotal
from a non-existent `bill.tax`, silently dropping GST from a document headed
"Tax Invoice"; it now reads the bill's own `subtotal` and itemises CGST/SGST.

**Two-layer doctor scheduling now exists from outside the process (P5).**
`DoctorScheduleService` was 330 lines, registered, and injected by nothing, with
zero routes. `doctor-schedules.controller.ts` exposes blocks, exceptions,
computed slots and capacity-checked booking under `/api/doctor-schedules`.

**The E2E suite can now fail (P6).** Two of its assertions were
`expect([200, 400, 403, 404]).toContain(res.status)` — true whether the guard
enforced, was permissive, or was absent. The bed test now drives a bed into
maintenance itself and asserts the illegal transition is rejected *and* that the
legal one is accepted; the ambulance test asserts a patient can read their own
request and is 403'd on someone else's. A blanket allow or a blanket deny now
fails one of each pair.

**The offline booking catalogue is generated, not hand-written (N5).**
`front-end/shared/mock-hospitals.js` had drifted to 15 doctors against the
backend's 48, leaving seven of eight hospitals with one department holding one
doctor. `back-end/scripts/generate-mock-hospitals.js` regenerates it from
`data/hospitals.json` + `data/users.json` — **re-run it after any reseed.**
Because every id in it is now a real record, `doctor-directory.js` falls back to
it again instead of blanking the wizard when the API is unreachable;
`window.doctorDirectoryLive` says which of the two is on screen. *This reverses
a deliberate choice made in `26f0b21` — the reasoning there was that the mock
listed hospitals and doctors that did not exist, which is no longer true.*

**Bill IDs (D1).** `generateBillId` matched only `BILL-0088`, so against the
reseed's `BILL-H001-001` nothing parsed and every new bill was `BILL-0001`. It
now parses and emits both schemes, hospital-scoped when the hospital is known.

**Swagger (S1–S5).** `docs/swagger.json` is no longer rewritten on every boot —
that churn hit four of the nineteen commits. Regenerate deliberately with
`npm run swagger:generate`. `main.ts` and `generate-swagger.ts` each carried
their own copy of the `DocumentBuilder`, which is how 14 in-use tags ended up
declared in neither; both now call `buildSwaggerConfig()` in
`src/swagger.config.ts` — **add a tag there when adding a controller.**
`HospitalDetailsController` is back under the `Hospitals` tag (it shares the
`hospitals` base path). The dead `x-user-role` API-key scheme is gone — the role
travels in the JWT. `GET /api/billing/:id/pdf` declares `application/pdf`, and
`POST /api/patients/:id/verify-insurance` has a real DTO, so `ValidationPipe`
no longer skips it.

### Middleware ↔ Swagger sync (the graded deliverable)

The audit only covered the six missing `ApiResponse` imports. The wider question
— does the Swagger document describe what the middleware actually does — it did
not ask, and the answer was no. Measured against the `.forRoutes(...)` wiring:

| Middleware | Applied to | Was documented on |
|---|---|---|
| `SecurityMiddleware` (429) | every route | 62 of 235 operations |
| `SecurityMiddleware` (413) | every route | 1 of 235 |
| `CsrfMiddleware` (403 + `x-csrf-token`) | every write | 0 of 100 writes |
| `RequestLoggerMiddleware` (`x-request-id`) | every route | nowhere |
| `AmbulanceAccessMiddleware` (403) | 10 routes | 3 of 10 |
| `HospitalAccessMiddleware` (403) | 10 routes | 5 of 10 |
| `BedStatusChangeMiddleware` (400) | 5 routes | 5 of 5 ✓ |
| `FileUploadMiddleware` (413) | `POST /uploads` | 1 of 1 ✓ |

`x-query-timestamp` was also declared with `@ApiHeader`, which renders it as a
**request** input — it is a **response** header set by `HospitalQueryInterceptor`.
Swagger UI was showing an input box for a header the client must never send.

**How it is fixed.** The global chain is folded into every operation by
`applyMiddlewareContract()` in `src/swagger.config.ts`, called from both
`main.ts` and `generate-swagger.ts`. Hand-decorating 235 operations is what
produced the build break in the first place, so the contract is applied once,
programmatically, from the middleware's **own exported constants** —
`GENERAL_LIMIT`, `AUTH_LIMIT`, `WINDOW_MS`, `MAX_BODY_BYTES`,
`MAX_UPLOAD_BYTES`, `CSRF_EXEMPT_ROUTES`, now exported from
`lodger.middleware.ts`. **The docs cannot claim a limit the middleware does not
enforce, because there is only one copy of each number.** Route-level middleware
keeps `@ApiResponse` on the routes it guards, where a reader will see it.

Now: 429 and 413 on 236/236 operations, `x-csrf-token` on 97/100 writes (the
three exempt ones are exactly `CSRF_EXEMPT_ROUTES`), middleware headers on every
response, and `x-query-timestamp` declared as a response header.

**Section 5 of `test/middleware-swagger.e2e-spec.ts` now machine-checks this.**
It builds the document the app really serves and asserts it against the
middleware wiring — including that the documented rate limits are the live
constants, and that the headers it promises are actually on a live response.
It caught two bugs in `applyMiddlewareContract` while being written (it assumed
the `/api` prefix, which the E2E app does not set). Suite is 13 tests, was 5.

### The no-show status — the judgement call, now decided

`noShowRate` drives up to a 30-point penalty in the hospital health score, but
compared against a `'no_show'` string that `AppointmentStatus` never defined, so
the component was dead and **every hospital's score was systematically too
high**. Removing it would have discarded a signal the team deliberately built,
so it is now real: `AppointmentStatus.NO_SHOW = 'No Show'`, set through
`PATCH /api/appointments/:id/no-show` (staff and doctors, never the patient).

A no-show records attendance, not anything clinical, so it sits inside the §1
non-clinical rule. It is distinct from `CANCELLED`: no consultation fee is
raised, and — like cancelled and completed — it releases the slot, so it is in
the duplicate-booking guard, the slot-conflict guard and the reconciliation
skip-lists. It is counted in `getStats()` and `getDoctorStats()`.

Note the seed's mirrored enum had to be updated alongside it, and the seed's
validation gate is what would have caught it otherwise — that check earns its
keep the first time the enum changes.



### Done on 2026-08-30 (third pass — merging with the team)

**Merged `origin/main` three times** as teammates kept pushing. Conflicts were
resolved by UNION wherever both sides had added things (`user.interface.ts`
fields, `users.controller.ts` routes, `hospitals.service.ts` methods,
`shared/api.js` objects), never by picking a side and dropping work.

**`back-end/dist/tsconfig.build.tsbuildinfo` is no longer tracked.** `dist/` is
gitignored, but that one artifact was committed, so it conflicted on every
single merge. It is regenerated by `npm run build`.

**CSRF is back on.** It had been unwired (`2d21f99 removed csrf`) because it was
answering 403 to *every* request including `POST /auth/login` — nobody could
sign in. That was a bug in the middleware, not a reason to drop the protection.
The fixed version exempts pre-session auth routes and Bearer-authenticated
writes (structurally immune — a browser never attaches an `Authorization`
header by itself) and challenges only unauthenticated state-changing requests,
which in practice means the public `POST /hospitals/register`. A teammate's
`CSRF_TOKEN_EXPIRATION` env var is kept.

**Bugs fixed that arrived from main:**

1. **`front-end/ambulance/app.js` was a syntax error on `origin/main`** — an
   `await` inside a non-`async` submit handler (line ~1645). The whole file
   failed to parse, so *nothing* on the ambulance portal ran. Handler is now
   `async`.
2. **`assignedManagerId` was set to hospital-manager ids** on H001–H003.
   Every backend reader treats that field as the REGIONAL OFFICER; hospital
   managers are already linked by their own `user.hospitalId`. Restored.
3. **Scope leak in `regional-officer/dashboard.js`** — `assignedManagerId ===
   'HM001' || === 'M001'` was OR'd into the filter to work around (2), which
   showed one officer's hospitals to every other officer. Gone.
4. `auth/change-password.html` referenced `logo.js` instead of `../logo.js`.

**Re-removed files that a blanket restore brought back**
(`e7b7f0a Restore files that were deleted during merge`):
the three orphaned `book-appointment-step*.html` pages (nothing links to them,
they reference an `app.js` and `styles.css` that do not exist) and
`patch_ambulance*.py`. All were deliberately deleted on 2026-08-28.

### Done on 2026-08-30 (second pass)

**Merged `origin/deekshitha`** — regional-manager assignment support. One
conflict, in `data/users.json`: her branch gave M001 `["Tirupati"]`, main already
had `["Tirupati","Renigunta"]` plus M002/M003. Kept the superset — HSP006 sits in
Renigunta and would otherwise have had no officer. She also deleted
`docs/swagger.json` and some tracked `dist/` files; the `dist/` deletions were
kept (that directory is gitignored and should never have been tracked),
`swagger.json` was kept in the tree because it is the API deliverable served at
`/api/docs`, and it has been regenerated to document her routes and mine.

**Her four RM endpoints were hardened** — snapshot reads, consistent envelope,
case-insensitive area matching. See §5 for what changed and why.

**`GET /revenue/regional-officers`** — revenue and operational data per regional
officer, plus the *Regional officers* tab in `superuser/revenue.html`. See §5A.

**The assignment dropdown now asks the backend.**
`superuser/hospital-registrations.js` used to filter officers client-side by
`areas`; it now calls `suggest/:city`, groups the dropdown into "Covers <city>"
and "Other areas", shows each officer's current load, and prints the top
recommendation under the control. One request per distinct city on the page,
cached for the render, with a client-side fallback so a failed suggestion call
never leaves the Admin with an empty dropdown.

### Done on 2026-08-30

**`Lab2-React/` removed from the repo.** All 25 tracked files deleted
(`git rm -r --cached` + `rm -rf`) and the directory added to `.gitignore` along with
two spelling variants, so it cannot be re-added by accident. §16 is gone with it.

**Doctors became login actors** — see §4. This reverses the 2026-08-28 decision;
that section is now the authority, not the old one.

**The hierarchy and visibility scope** — new `hierarchy` module, two portal pages,
three regional officers with `areas` — see §5B.

**The revenue model was rebuilt** from two streams to eight across three payers —
see §5A.

**Real bugs found and fixed along the way:**

1. **Staff self-registration was broken twice over.**
   `auth/staff-register.html` read `.form-control` inputs *positionally*, and the
   indices had drifted: `inputs[3]` and `inputs[4]` were Hospital ID and Department,
   not the two password boxes — so signup sent the hospital id as the password and
   compared it against the department. Every input now has an `id` and the handler
   reads by id. It also posted to `/auth/register` (the **patient** endpoint, which
   rejects a role and a `hospitalId`) instead of `/auth/register-staff`.
2. **The regional-officer assignment dropdown was always empty.** It only offers
   officers whose `areas` contain the hospital's city, and the single seeded officer
   had no `areas` array at all, so no hospital could ever be assigned.
3. **`H001.assignedManagerId` pointed at a hospital manager**, not a regional
   officer, which is what every reader of that field expects.
4. **The booking wizard's catalogue was fiction** — four of six hospitals and 26 of
   32 doctors did not exist in the database. See §4, `shared/doctor-directory.js`.
5. **Appointments had no `doctorId`**, so a booking could not reach a doctor's portal
   or earn consultation commission. See §4.
6. **`hospitals.regionalReviewStatus` was `undefined`** on every verified hospital;
   backfilled to `cleared`, which is what "verified" implies under the two-stage chain.

### Fixed on 2026-08-28

The 2026-08-28 sweep found these; they have all been dealt with. Kept here so
nobody "rediscovers" them or reverts a fix by accident.

**Backend would not compile** (`npm run build` emitted 3 TS errors while still
emitting output, so it looked like it worked):

1. `beds/beds.module.ts` imported `BedStatusChangeMiddleware` from
   `../lodger.middleware`, which does not export it → now imports from
   `./middleware/bed-status-change.middleware`.
2. `hospitals/hospitals.module.ts` had the same wrong import for
   `HospitalAccessMiddleware` → now `./middleware/hospital-access.middleware`.
3. `ambulance.service.ts:91` referenced `AmbulanceStatus.CANCELLED`, missing from
   the enum. **Added it** (every sibling status enum has one) and completed the two
   exhaustive `Record<AmbulanceStatus, …>` maps it cascaded into — `getStats()`'s
   counter and `isValidStatusTransition()`'s table. A trip may now be cancelled from
   Pending / Dispatched / En Route; once the patient is picked up it runs to
   completion. `Completed` and `Cancelled` are both terminal.
   Note: cancelling an ambulance request is still a **hard delete** in
   `ambulance.service.ts:161` (`splice`), so nothing writes `'Cancelled'` yet. The
   enum member exists for `PATCH /ambulance/:id/status` and for the duplicate check.

**Dead links** — `shared/nav.js` pointed at two files that never existed
(`administrative_staff/reports.html`, `patient/appointments.html`), and
`patient/appointments/appointments.html` linked its sidebar brand to `dashboard.html`
which resolved *inside* the appointments folder. All three fixed.

**Doctor login was removed on 2026-08-28 and deliberately restored on 2026-08-30.**
The radio in `auth/login.html`, the `case "doctor"` redirect, the `rolePathMap` entry
and `front-end/doctor/` are all back, this time with a backend that accepts them.
`superuser/manage-users.html` no longer labels the role "(directory record)" — only
Nurse carries that label now. Do not re-remove any of this; see §4.

**Duplicates and orphans removed** — `front-end/manager/` (superseded by
`hospital_manager/`, nothing linked to it); `front-end/hospital-registration/`
(consolidated onto `landing/hospital-registration.html`, which is the version that
sends the `adminPhone` the backend requires — `auth/login.html` and the
`session.js` public-path list were repointed); the three orphaned
`book-appointment-step*.html` pages (nothing outside them linked in, superseded by
the single-page wizard, and they referenced an `app.js` that never existed);
`back-end/.next/` (4 Next.js bundles from an abandoned experiment);
4 stray tracked files under the gitignored `back-end/dist/`;
`patch_ambulance*.py` (operated on a clone path that no longer exists);
`front-end/frontend_log.txt`.

**`TEST_ACCOUNTS.md` rewritten** — it claimed 12 seed accounts, 5 login actors,
7 doctors and 2 hospitals. Reality: **45 users, 26 login accounts, 19 doctors,
12 hospitals**. It also claimed wards/departments/equipment do not persist; they do.

### Still true, still deliberate

**`lodger.middleware.ts` is misspelt on purpose** — it is the real file.
`logger.middleware.ts` and the three `common/middleware/*.ts` files are one-line
re-export shims to it. Do not "fix" the name without updating all four shims,
`app.module.ts`, and `main.ts`. (The two bad imports fixed above were *not* this
file's fault — those middlewares genuinely live in their own feature folders.)

**Only superuser, regional-officer and doctor load `shared/nav.js`.** Every other
portal hardcodes its sidebar in HTML — which is why adding the patient Care+ link
meant editing six patient pages rather than one file. The `patient` and
`administrative_staff` branches of `nav.js` are still dead code, but their links are
correct, so wiring those portals up later is a one-line change.

**`Database/DBschema.sql` does not describe the running system.** It is a design
artefact with `Doctors`, `Treatments`, `Reminders`, `Alerts` tables and a
`MANAGEMENT` role that no longer exist in the model. The runtime store is
`back-end/data/*.json`.

**`administrative_staff/generate_pages.js`** (257 lines) is loaded by zero pages.
Left in place — it looks like a scaffolding helper someone may still want.

---

## 15. Conventions, commands, gotchas

### Conventions
- Commits: Conventional Commits (`feat:`, `fix:`, `build:`).
- Branch per member (`vivian`, `nikitha`, `poorna`, `deekshitha`, `Poorvishree`),
  merged to `main`.
- **Never add a Claude co-author trailer to commits.**
- `back-end/dist/` and `back-end/docs/swagger.json` are generated — rebuild rather
  than hand-edit.

### Running it

```bash
# Backend (port 3001)
cd back-end
npm install
npm run start:dev          # watch mode
npm run start:prod         # node dist/src/main
npx jest                   # 5 suites, 19 tests
npm run build              # regenerate dist/

# Frontend (any static server; the API client mirrors the hostname on :3001)
cd front-end
npx serve -l 8080
```

Swagger UI: `http://localhost:3001/api/docs`.
**Just start the server to regenerate the docs** — `main.ts` writes
`docs/swagger.json` during bootstrap, so it is always current after a run, and that
is the authoritative version. Prefer it over `generate-swagger.ts`.

### Env vars
`PORT` (3001) · `JWT_SECRET` (warns if unset or left at the shipped default) ·
`CORS_ORIGIN` (comma list; default reflects any origin) · `MAX_BODY_BYTES` (1 MB) ·
`MAX_UPLOAD_BYTES` (5 MB) · `API_URL`. Loaded by a hand-rolled parser in `main.ts`;
`.env` is gitignored.

### Gotcha: running the backend dirties the repo
`docs/swagger.json` is rewritten on every boot. Check the diff is real before
committing.

### Gotcha: login rewrites the password before authorising
`AuthService.login` upgrades a legacy plaintext password to a scrypt hash *before*
it checks the role. A login that is ultimately rejected still rewrites that user's
stored password in `data/users.json`. Harmless (the password was verified correct
first), but it means poking at auth dirties the seed data — check
`git diff back-end/data/` before committing.

### Gotcha: static hosts eat query strings
`npx serve` answers `/page.html?x=1` with a `301 → /page` and **drops the query
string**. Always build in-app links with `window.pageLink(page, params)` from
`shared/api.js` instead of hardcoding `.html`.

### Gotcha: rates are fractions, percentages are a UI concern
`commissionRate` and `paymentGatewayRate` are stored as fractions — `0.019` is 1.9%.
`superuser/revenue.js` converts on the way in and out (`FEE_FIELDS` marks which
fields are `percent`). Sending `1.9` to `PATCH /revenue/fees` would charge 190%;
`PricingService.updateFeeConfig` rejects a gateway rate above 1 for exactly that reason.

### Gotcha: patient revenue is keyed on the patient record, not the login
Appointments and bills carry `patientId` (`P001`), not the user id (`U020`).
`RevenueController.patientKey()` maps one to the other. Key a membership on the wrong
one and a member's own bookings never match their fee waiver.

### Gotcha: method-level `@Roles` replaces, not extends
`RolesGuard` uses `reflector.getAllAndOverride`. A method-level `@Roles(...)` wipes
out the class-level list entirely — so a method list must repeat every role that
should still have access. This is how read access was granted to `regional_manager`
without granting writes.
